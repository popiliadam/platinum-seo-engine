# Manager Session Brief — v1.3 Slash Command Expansion (9 → 15)

**Yazım tarihi:** 2026-05-06 (post-v1.x audit closure 13/13 RESOLVED, engine HEAD `da9653f` PUSHED)
**Manager session role:** 6 yeni slash command create + atomic batch commit + push.
**Estimated duration:** ~45 dk (6 command × 4-5 dk + acceptance verify + closeout 10 dk)

---

## 🎯 Hedef

VS Code Claude Code palette'inde Süleyman'ın en sık kullanacağı **6 yeni slash command** oluştur. Mevcut 9 command + 6 yeni = **15 command** (Hybrid Seçenek B). Her command **mevcut bir skill'e doğrudan bağlanır**, skill body + scripts + MCP cross-references çok efektif kullanılır.

**KRİTİK:**
- Engine v1.x release line tamamen kapanmış (13/13 audit RESOLVED + 2 ADR closed + 3.1GB drift cleanup)
- 43 skill `status: active` (Phase B status promote sonrası production-ready)
- Plugin marketplace publication ready (commit `ee2d67a` marketplace.json valid)
- 4 MCP server connected: gsc + dataforseo + ScraplingServer + higgsfield (user-level)
- Engine HEAD baseline `da9653f` PUSHED, working tree clean

Bu commands **Süleyman ergonomi expansion** — palette'te kısa yol + kavramsal netlik. 43 skill'in tümü doğal dil ile zaten erişilebilir; bu 6 command **en sık kullanılan** subset için. v1.3 milestone başlangıcı (yeni feature work scope).

---

## 📋 Wakeup Sequence (zorunlu okuma sırası)

1. `memory/MEMORY.md` + `memory/project_current_status.md` — Phase B post-closeout state (13/13 audit RESOLVED, engine HEAD da9653f, 9 commit advance, plugin marketplace ready)
2. `docs/PHASE_STATUS.md` — son banner: "v1.2-Phase-B-Audit-Followup COMPLETE — v1.2 SEO domain milestone NEXT"
3. `docs/OPEN_QUESTIONS.md` — son commit'ler + RESOLVED markerları (Q-V1.2-OPP-COVERAGE-01 + Q-V1.2-MASTER-TASK-PRIMARY-SOURCE-01 + Q-RP-01 RESOLVED)
4. `commands/pseo-quickwin.md` — **mevcut command paterni (skill chain)** — referans format için zorunlu
5. `commands/pseo-init.md` — **mevcut command paterni (subprocess wrap)** — referans format için zorunlu
6. `commands/pseo-driftcheck.md` — **mevcut command paterni (skill chain governance)** — referans format için
7. `commands/pseo-monthly.md` — **mevcut command paterni (reporting)** — referans format için
8. `skills/production/new-blog/SKILL.md` — Fix #1 source (~480L, R-71..R-76 enforce)
9. `skills/reporting/monitoring-weekly/SKILL.md` — Fix #2 source (Phase B Wave 3 inline orchestration, 3 Python block)
10. `skills/meta/whats-next/SKILL.md` — Fix #3 source (Phase 5+ meta routing, append_work)
11. `skills/discovery/schema-audit/SKILL.md` — Fix #4 source (`mcp__dataforseo__on_page_content_parsing`, schema_audit_transform.py)
12. `skills/discovery/cannibalization/SKILL.md` — Fix #5 source (`mcp__gsc__search_analytics`, cannibalization_transform.py)
13. `skills/discovery/content-decay/SKILL.md` — Fix #6 source (`mcp__gsc__enhanced_search_analytics`, content_decay_transform.py)
14. `.claude-plugin/plugin.json` — plugin manifest (commands directory pointer)
15. `.claude-plugin/marketplace.json` — marketplace metadata (plugin description "v1.1.0 — 43 skills, 19 schemas, 18 rules, 9 commands" → güncellenmeli "15 commands")
16. `README.md` — command listesi var mı kontrol (varsa sync gerek)

---

## 🌊 6 Command Detail (Each ~30-50 satır body)

### Fix #1 — `/pseo-new-blog` (Production Content Generation)

**Skill mapping:** `skills/production/new-blog/SKILL.md` (active, Phase 11 W1)

**Body kapsamı:**
- Argüman: `<project-slug> <new-content-plan-id> [--mode draft|publish]`
- Skill chain:
  1. Aktif proje çöz (`shared/active.json` veya `$1`)
  2. Skill `new-blog` tetiklenir → SKILL.md 8-step protocol:
     - Step 1: master.xlsx[new_content_plan] row read (id=$2)
     - Step 2: project-config consume (brand_identity + content_settings)
     - Step 3: R-71..R-76 enforce (8K ultra realistic, nano-banana, hero image, picture LCP, format cascade, alt text)
     - Step 4: Content generation (markdown + HTML + JSON-LD + meta-tags)
     - Step 5: Foundational Principles 3-layer enforce (Truth-Verifiable + Profile-Aware + AI-Suistimal)
     - Step 6: Output write — outputs/blog/{slug}/article.{md,html,jsonld} + meta-tags.json + upload-instructions.md
     - Step 7: events.jsonl append — event_kind=work + event_type=content_new + task_id minted (T-XXXXX)
     - Step 8: Onay gate (workflow-run.schema awaiting_approval)
- DURUR conditions (6 sentinel) inline reference

**Scripts cross-reference:**
- `scripts/state/events_writer.py` — append_work convenience wrapper
- `scripts/state/workflow_runner.py` — onay gate state machine
- `scripts/excel/transaction.py` — master.xlsx row update (mode=publish)
- `scripts/reporting/render_template.py` — markdown/HTML render helper

**MCP tools:**
- `mcp__higgsfield__generate_image` (required for hero image, R-72 nano-banana default)
- `mcp__higgsfield__job_status` (optional, polling)

**Cross-references:**
- Templates: `templates/content/new-blog.template.{md,html}` + `templates/content/upload-instructions.template.md`
- Rules: `rules/content-quality.md` + `rules/content-html-discipline.md` + `rules/content-seo-discipline.md` + `rules/content-eeat-discipline.md` + `rules/content-llm-discipline.md` + `rules/content-update-discipline.md`
- Schemas: `schemas/master-excel.schema.json#new_content_plan` + `schemas/events.schema.json` (URL-bearing work event constraints: url+url_normalized+after+pillar)

**Frontmatter pattern (commands/pseo-new-blog.md):**
```yaml
---
description: |
  Use when: kullanıcı "yeni blog yaz", "blog üret", "new content", "İzmir implant fiyatları yaz" der ya da `/pseo-new-blog` çağırırsa.
  Also use when: aktif projede `new_content_plan` sheet'inde bekleyen NCP row var; pillar content production tetiklenmek isteniyor.
  Do not use when: mevcut blog revize (`/pseo-revise-content`), FAQ ekleme (`/pseo-faq-optimization`), content remediation (`/pseo-content-remediation`).
argument-hint: "<project-slug> <new-content-plan-id> [--mode draft|publish]"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

### Fix #2 — `/pseo-monitoring-weekly` (Weekly Health Check)

**Skill mapping:** `skills/reporting/monitoring-weekly/SKILL.md` (active, Phase 12 W2 + Phase B Wave 3 inline orchestration fix)

**Body kapsamı:**
- Argüman: `<project-slug> [--week-start YYYY-MM-DD] [--week-end YYYY-MM-DD]`
- Skill `monitoring-weekly` tetiklenir → SKILL.md 8-step + 3 inline Python block (Phase B Wave 3):
  - Block 1: Setup + read drift-check `consistency-report.json` (DURUR #3 fallback)
  - Block 2: Read `shared/portfolio.json` + per-project metrics aggregate + severity compute
  - Block 3: Markdown render + `events_writer.append_audit` (event_kind=audit, audit_target=reports:monitoring-weekly:{week})
- Sonuç: `outputs/reports/{date}-monitoring-weekly.md` (1700+ byte) + audit row events.jsonl'a

**Scripts cross-reference:**
- `scripts/state/events_writer.py` — append_audit convenience wrapper
- `scripts/reporting/render_template.py` — string.Template $var substitution

**MCP tools:**
- (none) — local aggregation, no MCP, no DFS, no budget pre-flight

**Cross-references:**
- Templates: `templates/reports/monitoring-weekly.template.md` (DURUR #4 inline fallback if missing)
- Rules: `rules/events-writer.md` Section 4c (audit-only events, event_type YASAK per Section 6)
- Schemas: `schemas/events.schema.json` (audit if/then required: audit_action + audit_target + actor)

**Frontmatter pattern:**
```yaml
---
description: |
  Use when: kullanıcı "haftalık check", "weekly health", "haftalık monitoring", "haftalık sağlık raporu" der ya da `/pseo-monitoring-weekly` çağırırsa.
  Also use when: Pazartesi 09:00 UTC cron-like trigger; manager portfolio-overview öncesi tek-proje sağlık özeti istiyor.
  Do not use when: ay sonu rapor (`/pseo-monthly`), portföy genel rapor (portfolio-overview skill), drift kontrol (`/pseo-driftcheck`).
argument-hint: "<project-slug> [--week-start YYYY-MM-DD] [--week-end YYYY-MM-DD]"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

### Fix #3 — `/pseo-whats-next` (Meta Routing — "Sıradaki Ne?")

**Skill mapping:** `skills/meta/whats-next/SKILL.md` (active, Phase 5+ meta routing)

**Body kapsamı:**
- Argüman: `<project-slug> [--user-intent "metin"]`
- Skill `whats-next` tetiklenir → SKILL.md routing protocol:
  - Workflow runs scan (workflow-run.schema awaiting_approval)
  - Master_task TODO rows scan (priority sorting)
  - Recent events scan (events.jsonl last 7 day)
  - Recommendation scoring (top-3 ranked)
  - events_writer.append_work (event_kind=work + event_type=manual + task_id synthetic mint + note=markdown bullet list)
- Output: structured ranking markdown (top-3 recommendation)

**Scripts cross-reference:**
- `scripts/state/events_writer.py` — append_work convenience wrapper (synthetic task_id mint paterni: `T-{90000 + (int(hashlib.sha1(run_id).hexdigest()[:4], 16) % 9999)}`)
- `scripts/state/workflow_runner.py` — workflow run state read

**MCP tools:**
- (none) — pure local routing logic

**Cross-references:**
- Schemas: `schemas/events.schema.json` (work if/then required: event_type + task_id; manual event_type → note required)
- Schemas: `schemas/workflow-run.schema.json` (run_id pattern)

**Frontmatter pattern:**
```yaml
---
description: |
  Use when: kullanıcı "sıradaki ne", "what next", "next step", "ne yapayım", "öncelik nedir" der ya da `/pseo-whats-next` çağırırsa.
  Also use when: yeni session açılışı sonrası agenda tetikleyicisi; multiple workflow paralel awaiting_approval halinde manager karar vermesi gerekir.
  Do not use when: spesifik skill çağrısı (`/pseo-quickwin`, `/pseo-monthly`, vs); sadece ranking değil action gerekli.
argument-hint: "<project-slug> [--user-intent 'natural language metin']"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

### Fix #4 — `/pseo-schema-audit` (Schema Markup Audit)

**Skill mapping:** `skills/discovery/schema-audit/SKILL.md` (active, Phase 8)

**Body kapsamı:**
- Argüman: `<project-slug> [--url-list <path>] [--budget-credits N]`
- Skill `schema-audit` tetiklenir → SKILL.md 8-step:
  - Step 1: master.xlsx[content_inventory] read (target URLs)
  - Step 2: budget pre-flight (`scripts/budget/check_budget.py`)
  - Step 3: `mcp__dataforseo__on_page_content_parsing` per URL
  - Step 4: Pure compute via `scripts/discovery/schema_audit_transform.py`
  - Step 5: master.xlsx `schema` sheet write (transaction.py)
  - Step 6: events.jsonl append (event_kind=provenance + operation=ingest + source.kind=dataforseo_mcp)
  - Step 7: Onay gate (workflow-run.schema)
  - Step 8: outputs/reports/{date}-schema-audit.md (render_template)

**Scripts cross-reference:**
- `scripts/discovery/schema_audit_transform.py` — pure compute (JSON-LD parse, schema.org type validate)
- `scripts/budget/check_budget.py` — pre-flight DataForSEO credit budget
- `scripts/state/events_writer.py` — append_provenance
- `scripts/excel/transaction.py` — master.xlsx schema sheet write
- `scripts/reporting/render_template.py` — markdown render

**MCP tools:**
- `mcp__dataforseo__on_page_content_parsing` (required, schema markup extraction)

**Cross-references:**
- Templates: `templates/reports/schema-audit.template.md`
- Rules: `rules/schema-first.md` + `rules/budget-events.md`
- Schemas: `schemas/master-excel.schema.json#schema`, `schemas/dataforseo-mapping.schema.json`

**Frontmatter pattern:**
```yaml
---
description: |
  Use when: kullanıcı "schema audit", "schema markup kontrol", "JSON-LD audit", "structured data drift" der ya da `/pseo-schema-audit` çağırırsa.
  Also use when: aktif projede `content_inventory` sheet'inde URL'ler var; düzenli schema markup health check tetiklenmek isteniyor.
  Do not use when: schema validation (`/pseo-driftcheck` schema-validate skill çağırır), tech-audit (`tech-audit` skill ayrı), on-page audit (`on-page-audit` skill ayrı).
argument-hint: "<project-slug> [--url-list <path>] [--budget-credits N]"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

### Fix #5 — `/pseo-cannibalization` (Query Cannibalization Detection)

**Skill mapping:** `skills/discovery/cannibalization/SKILL.md` (active, Phase 7)

**Body kapsamı:**
- Argüman: `<project-slug> [--days-back 90]`
- Skill `cannibalization` tetiklenir → SKILL.md 8-step:
  - Step 1: project-config gsc.site_url consume
  - Step 2: `mcp__gsc__search_analytics` raw 5000-row query×page pivot (last 90 day)
  - Step 3: Pure compute via `scripts/discovery/cannibalization_transform.py` (conflict detection)
  - Step 4: master.xlsx `cannibalization` sheet write
  - Step 5: events.jsonl append (event_kind=provenance + operation=ingest + source.kind=gsc_mcp)
  - Step 6: Onay gate
  - Step 7: outputs/reports/{date}-cannibalization.md (top conflict pair + total impact + resolution)
  - Step 8: Optional remediation chain (content-remediation skill suggestion)

**Scripts cross-reference:**
- `scripts/discovery/cannibalization_transform.py` — pure compute (query overlap + URL conflict matrix)
- `scripts/state/events_writer.py` — append_provenance
- `scripts/excel/transaction.py` — master.xlsx cannibalization sheet write
- `scripts/reporting/render_template.py` — markdown render

**MCP tools:**
- `mcp__gsc__search_analytics` (required, GSC raw query data)
- `mcp__gsc__enhanced_search_analytics` (optional, alternative data source)

**Cross-references:**
- Templates: `templates/reports/cannibalization.template.md`
- Schemas: `schemas/master-excel.schema.json#cannibalization`, `schemas/gsc-mapping.schema.json`

**Frontmatter pattern:**
```yaml
---
description: |
  Use when: kullanıcı "kanibalizasyon", "cannibalization", "query çakışması", "URL conflict", "iki sayfa aynı keyword'de yarışıyor" der ya da `/pseo-cannibalization` çağırırsa.
  Also use when: GSC verisi 90 gün+ retrospective; multiple URLs ranking for same query detection regular check.
  Do not use when: tek URL audit (`on-page-audit` skill), content gap analizi (`content-gaps` skill), competitive analysis (`competitive-analysis` skill).
argument-hint: "<project-slug> [--days-back 90]"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

### Fix #6 — `/pseo-content-decay` (Content Decay Detection)

**Skill mapping:** `skills/discovery/content-decay/SKILL.md` (active, Phase 7)

**Body kapsamı:**
- Argüman: `<project-slug> [--recent-days 30] [--baseline-days 30]`
- Skill `content-decay` tetiklenir → SKILL.md 8-step:
  - Step 1: project-config gsc.site_url consume
  - Step 2: `mcp__gsc__enhanced_search_analytics` recent window
  - Step 3: `mcp__gsc__enhanced_search_analytics` previous baseline window
  - Step 4: Pure compute via `scripts/discovery/content_decay_transform.py` (delta regression detect)
  - Step 5 (optional): `mcp__dataforseo__dataforseo_labs_google_historical_rank_overview` (budget-gated)
  - Step 6: master.xlsx `content_decay` sheet write
  - Step 7: events.jsonl append (event_kind=provenance + operation=ingest + source.kind=gsc_mcp)
  - Step 8: Onay gate + outputs/reports/{date}-content-decay.md (HIGH severity decay revision plans for revise-content skill chain)

**Scripts cross-reference:**
- `scripts/discovery/content_decay_transform.py` — pure compute (week-over-week regression + 5σ anomaly threshold)
- `scripts/budget/check_budget.py` — DataForSEO optional pre-flight
- `scripts/state/events_writer.py` — append_provenance
- `scripts/excel/transaction.py` — master.xlsx content_decay sheet write
- `scripts/reporting/render_template.py` — markdown render

**MCP tools:**
- `mcp__gsc__enhanced_search_analytics` (required, time-series GSC data)
- `mcp__dataforseo__dataforseo_labs_google_historical_rank_overview` (optional, paid DFS, budget-gated)

**Cross-references:**
- Templates: `templates/reports/content-decay.template.md`
- Schemas: `schemas/master-excel.schema.json#content_decay`

**Frontmatter pattern:**
```yaml
---
description: |
  Use when: kullanıcı "içerik gerileme", "content decay", "trafik düşüşü", "ranking decay" der ya da `/pseo-content-decay` çağırırsa.
  Also use when: aylık ritmik content health check; revise-content skill chain'i için HIGH severity tetikleyici toplama.
  Do not use when: kanibalizasyon detection (`/pseo-cannibalization`), tek URL audit (`on-page-audit` skill), schema markup drift (`/pseo-schema-audit`).
argument-hint: "<project-slug> [--recent-days 30] [--baseline-days 30]"
allowed-tools: Bash(jq:*), Bash(python3:*), Read
model: sonnet
---
```

---

## 🌊 Atomic Strategy

**Önerim: Tek atomic commit (6 command + plugin manifest sync + README).**

Niye? 6 command semantically related (slash command expansion batch) + atomic phase paterni 32'inci kanıt 35 phase consecutive convergent invariant intact (Phase B paterni reuse). Ayrı per-command commit overkill — semantic chunk düşük risk + uniform pattern (frontmatter + 4-section body).

**Single atomic commit message:**
```
feat(commands): 6 slash command expansion 9→15 (v1.3 ergonomi batch)

Süleyman ergonomi expansion — VS Code Claude Code palette'inde kısa yol +
en sık kullanılan skill'lere doğrudan erişim.

6 yeni command (each ~50-80 satır):
- /pseo-new-blog → skills/production/new-blog (Phase 11 W1, R-71..R-76 enforce)
- /pseo-monitoring-weekly → skills/reporting/monitoring-weekly (Phase 12 W2 + Phase B Wave 3 inline orchestration)
- /pseo-whats-next → skills/meta/whats-next (Phase 5+ meta routing)
- /pseo-schema-audit → skills/discovery/schema-audit (Phase 8, dataforseo MCP)
- /pseo-cannibalization → skills/discovery/cannibalization (Phase 7, gsc MCP)
- /pseo-content-decay → skills/discovery/content-decay (Phase 7, gsc MCP + optional dfs)

Plus marketplace metadata sync:
- .claude-plugin/marketplace.json: "9 commands" → "15 commands" description update

Acceptance:
- 6 yeni commands/*.md dosya valid frontmatter + body 4-section
- Pytest 676 passed + 8 skipped baseline unchanged (commands no impact tests)
- .mcp.json byte-byte korundu (F-16 invariant 23+ commit)
- DECISIONS.md unchanged (no new ADR — slash command expansion governance polish)

Atomic phase paterni 33'üncü kanıt 36 phase consecutive convergent invariant intact.
```

**Push:** Süleyman onay sonrası `git push origin main`.

---

## ✅ Acceptance Criteria

1. ✅ 6 yeni `commands/*.md` dosya created (valid YAML frontmatter + 4-section body)
2. ✅ Each command frontmatter has: `description` (Use/Also/Do not when 3-clause) + `argument-hint` + `allowed-tools` + `model: sonnet`
3. ✅ Each command body 4-section: 1. Aktif proje çöz, 2. Skill chain detail, 3. Çalıştırma notları, 4. Bağımlılıklar
4. ✅ Skill cross-reference path correct (each command points to existing skill SKILL.md)
5. ✅ Scripts cross-reference complete (transform module + helpers + render_template)
6. ✅ MCP tools list correct (gsc + dataforseo + ScraplingServer + higgsfield mapping per skill)
7. ✅ Plugin agnostik discipline: no project slug literal in any command body (PSEO_PROJECT_ID env or `$1` arg)
8. ✅ pytest 676 passed + 8 skipped baseline unchanged
9. ✅ `.claude-plugin/marketplace.json` description "9 commands" → "15 commands" sync
10. ✅ Atomic commit + push (Süleyman onay sonrası)
11. ✅ Manuel test: VS Code Claude Code restart sonrası palette'te `/pseo-` ile arama → 15 command görünür
12. ✅ Smoke test: `/pseo-monitoring-weekly` çalıştır → workspace demo-dental'da markdown sample üretir (Phase B Wave 3 paterni reuse kanıt)

---

## 🚫 Constraints

- **Plugin agnostik discipline** — Slug literal yasak (PSEO_PROJECT_ID env veya `$1` arg ile resolve); F-16 invariant 23+ commit cumulative korunur
- **No new ADR** — slash command expansion governance polish, no schema bump, no architecture decision
- **No new MCP server** — mevcut 4 server (gsc + dataforseo + ScraplingServer + higgsfield user-level) intact, .mcp.json byte-byte korunur
- **No skill body modification** — skill SKILL.md'leri dokunulmaz; commands sadece skill referans + invocation
- **No new test** — 6 command markdown content; pytest baseline unchanged (commands plugin loader tarafından parse edilir, pytest fixture eklenmez)
- **README.md sync** — eğer command listesi varsa update; yoksa unchanged
- **Süleyman karar gateleri** — pre-dispatch 3 soru + push timing
- **Lesson 38 v2 enforcement** — runtime kanıtla brief premise doğrula, frozen assumption YASAK (mevcut 9 command paterni cross-check ZORUNLU)
- **Lesson 67 enforcement** — agent worker output ≠ kanıt; karar verici doğrudan verify before commit (commands/*.md valid YAML check + skill path exists check)

---

## 🤔 Süleyman'a Sorulacaklar (manager pre-dispatch, ~5 dk)

### Pre-dispatch (3 soru)

1. **Command isim tercihi:** Önerim `/pseo-new-blog`, `/pseo-monitoring-weekly`, `/pseo-whats-next`, `/pseo-schema-audit`, `/pseo-cannibalization`, `/pseo-content-decay` (kebab-case skill name'le birebir). Daha kısa isim ister misin (örn `/pseo-blog`, `/pseo-monitor`, `/pseo-next`, `/pseo-schema`, `/pseo-cannib`, `/pseo-decay`)?

2. **Body content depth:** Önerim her command body ~50-80 satır (pseo-quickwin/pseo-init paterni reuse — 4-section: aktif proje çöz + skill chain + çalıştırma notları + bağımlılıklar). Daha minimal (örn 20 satır one-liner each) veya daha detaylı (örn 100+ satır step-by-step doc) ister misin?

3. **Push timing:** Atomic batch sonrası tek push (önerim, Phase B paterni reuse) vs per-command push (6 push)?

### Wave-end onay noktaları

- Pre-create kanıt: 6 commands/*.md content draft sun → Süleyman onay → write tool ile commit
- Post-create kanıt: pytest 676 PASS unchanged + 6 commands/*.md valid YAML check + skill path exists check
- Push timing onayı

---

## ⏱️ Estimated Time

- Wakeup: 5 dk
- Pre-dispatch: 5 dk
- Wave 1 (6 command create + marketplace.json sync): 30 dk (6 × ~5 dk + marketplace update + README check)
- Acceptance verify: 5 dk
- Closeout (atomic commit + push + memory update): 10 dk
- **Total: ~55 dk**

---

## 📊 Success State (post-v1.3-batch)

- **15 slash command** in palette (was 9): pseo-active + pseo-dfs-pull + pseo-driftcheck + pseo-gsc-pull + pseo-init + pseo-monthly + pseo-quickwin + pseo-scrape + pseo-status + **pseo-new-blog + pseo-monitoring-weekly + pseo-whats-next + pseo-schema-audit + pseo-cannibalization + pseo-content-decay**
- **43/43 skill** still active (commands point to skills, no skill modification)
- **pytest 676 passed + 8 skipped** (baseline unchanged, no test impact)
- **.mcp.json byte-byte korundu** (F-16 invariant 23+ commit cumulative)
- **DECISIONS.md** unchanged (no new ADR)
- **Plugin marketplace publication** ready (marketplace.json description sync)
- **Süleyman ergonomi**: en sık kullanılan 6 skill'e doğrudan slash command kısa yol; geri kalan 37 skill doğal dil ile

---

## 🧠 Context Notes (Manager Reference)

### Mevcut 9 Command Pattern (Reference)

`commands/pseo-quickwin.md` (skill chain örnek):
```yaml
---
description: |
  Use when: ...
  Also use when: ...
  Do not use when: ...
argument-hint: "[project-slug]"
allowed-tools: Bash(jq:*), Read
model: sonnet
---

# /pseo-quickwin — GSC Quick-Wins Chain

> **Skill chain:** `skills/ingestion/gsc-pull/SKILL.md` (Phase 6) → `skills/discovery/quick-wins/SKILL.md` (Phase 5).

## 1. Aktif projeyi çöz
[bash inline: shared/active.json read or $1]

## 2. Skill chain
[skill spec detail]

## 3. Çalıştırma notları
[edge cases + manual pre-flight]

## 4. Bağımlılıklar
[skill paths + MCP servers + project.config requirements]
```

Bu paterni 6 yeni command'a uniform şekilde uygula.

### Plugin Manifest Sync

`.claude-plugin/marketplace.json` (current commit `ee2d67a`):
```json
{
  "plugins": [
    {
      "name": "platinum-seo-engine",
      "description": "v1.1.0 — 43 skills, 19 schemas, 18 rules, 9 commands, 4 hooks, 3 MCP servers (gsc + dataforseo + ScraplingServer). Production-ready (drift-check AMBER, pytest 673 PASS, 9 ADR codified)."
    }
  ]
}
```

Update target:
```json
{
  "description": "v1.3.0 — 43 skills, 19 schemas, 18 rules, 15 commands, 4 hooks, 3 MCP servers (gsc + dataforseo + ScraplingServer). Production-ready (drift-check GREEN candidate, pytest 676 PASS + 8 skip, 13/13 audit findings RESOLVED + 2 ADR closed)."
}
```

### Workspace Smoke Test (Acceptance Criteria #12)

VS Code Claude Code restart sonrası:
1. Cmd+Shift+P → Claude Code skip
2. Chat'e `/pseo-` yaz → 15 command'ı palette'te gör
3. `/pseo-monitoring-weekly demo-dental` çalıştır → smoke test:
   - Skill `monitoring-weekly` tetiklenir
   - 3 Python block sequential exec (Phase B Wave 3 paterni)
   - Markdown sample workspace/projects/demo-dental/outputs/reports/{date}-monitoring-weekly.md (1700+ byte)
   - events.jsonl audit-row append (event_kind=audit, audit_target=reports:monitoring-weekly:{week})
4. **Smoke test PASS** = ergonomi expansion gerçek çalışır kanıt

---

## 📚 Reference Docs

- **Atomic phase paterni cumulative kanıt:** Phase 7..15 + v1.1-Wave-1+2+3 + v1.1-Integration-Audit + Phase A + Phase B Wave 1+2+3+closeout + Cleanup 1+2 + Status Promote + #2 schema + #1 F-16 + ADR closure + Q-RP-01 = **35 phase consecutive atomic dispatch invariant intact**
- **Lesson 38 v2 enforcement:** 13'üncü ardışık production-ready (Phase B post-closeout 5 ardışık vaka)
- **Lesson 67 stacked enforcement:** 4'üncü ardışık vaka Phase B post-closeout (workspace state runtime check + F-16 implementation bug catch + slug literal catch via test failure)
- **Schema-first override paterni:** 18'inci uygulama (Section 4c 8-skill batch promote)
- **v1.x release line:** 13/13 audit findings RESOLVED + 2 ADR closed + 3.1 GB drift cleanup + plugin marketplace publication ready

**Engine HEAD baseline:** `da9653f` PUSHED (Phase B Wave 1+2+3+closeout + Cleanup 1+2 + Status Promote + Q-V1.2-MASTER-TASK + Q-V1.2-OPP-COVERAGE + ADR closure + Q-RP-01 = 9 commit cumulative).

**Workspace HEAD baseline:** `05278bb` PUSHED (Phase B post-closeout snapshot, 19 file changes 1708 insertions).

---

## 💡 Hatırlatma — v1.3 Milestone Başlangıcı

Bu **v1.3 milestone** başlangıcı (yeni feature work scope). v1.x release line tamamen kapanmış (13/13 audit + 2 ADR + drift cleanup), v1.3+ artık **özellik genişletme** scope'u.

Bu brief 6 slash command expansion atomic batch — v1.3 milestone içinde **ilk feature work** (ergonomi). Sonraki v1.3+ feature işleri:
- Plugin marketplace publication (gh repo public + Claude Code marketplace listing) — ayrı session
- Yeni proje onboard pipeline (multi-project capability test) — ayrı session
- v1.3+ feature backlog (Süleyman karar) — ayrı session

**Süleyman feedback Phase B confirm enforce:** "sen kendi için cross check yap sürekli" — sürekli runtime cross-check + Lesson 38 v2 + Lesson 67 her wave enforce default.

---

**Başla.** Wakeup sequence + 3 pre-dispatch sorusu sonrası Süleyman onayıyla 6 command create + atomic batch commit + push.

**Reference:**
- v1.2 Phase B brief paterni: `docs/superpowers/plans/v1.2-phase-b-fix-brief.md` (~155 dk single session 4 atomic commit + closeout, 12/13 audit RESOLVED)
- Phase A precedent: engine commit `6497ef4` (4 trivial fix atomic batch — paterni reuse)
- Mevcut 9 commands/*.md (frontmatter + body 4-section paterni reuse)
