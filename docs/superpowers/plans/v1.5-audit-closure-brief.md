# Manager Session Brief — v1.5 Audit Closure (2026-05-07 scripts audit findings)

**Yazım tarihi:** 2026-05-07 (post-v1.4.0 marketplace publication wave 1, engine HEAD `a6a4010` PUSHED to origin/main)
**Manager session role:** 2026-05-07 scripts audit'in kalan 10 finding + 7 yeni script önerisini 3 atomic milestone phase altında çöz. Quality discipline non-negotiable: TDD red-green her task için + Lesson 38 v2 cross-check + atomic commit per phase + memory advance per milestone-end + runtime grep verify her wave-end.
**Estimated total duration:** ~6-9 saat (3 phase × 90-180 dk + closeouts) — **3 ayrı fresh session önerilir**, 1 tek session'a sıkıştırma YASAK (kalite kaybı garantili).

---

## 🎯 Hedef

`docs/RELEASE_NOTES_v1.4.0.md` Section 6'da listelenen ve **2026-05-07 scripts audit raporunda** sıralanan 10 finding + 7 yeni script önerisini çözmek. Her finding **kendi atomic phase'inde**, kendi TDD cycle'ında, kendi cross-check verify'ında ele alınmalı — bundling YASAK.

**Sınır:** v1.5 milestone scope **bu brief**. v1.5+ milestone (örn: H-E events.schema event_type enum bump, Wave 3+4 marketplace publication PUBLIC) **AYRI brief'ler** ve **AYRI session'lar** gerektirir.

**KRİTİK invariantlar (her phase boyunca):**
- pytest baseline 970 PASS + 11 skip — **regression sıfır zorunlu**
- `.mcp.json` 482B byte-byte korunma F-16 invariant — 30+ commit cumulative
- ADR-036 5-file sync 1.4.0 invariant intact (yeni release notes/version bump v1.5'e kadar YASAK; bu brief'ler v1.4.0 baseline'ında çalışır, plugin.json bump v1.5 final commit'inde olur)
- DECISIONS.md 6126/6144B — 18B headroom; yeni ADR yazılmaması ÖNERİLİR (mevcut ADR-009/021/035 paterni reuse)
- Plugin agnostik discipline: F-16 invariant — engine repo path workspace içinde olmamalı, 0 slug literal

---

## 📋 Wakeup Sequence (zorunlu okuma sırası)

1. **`memory/MEMORY.md`** + **`memory/project_current_status.md`** — v1.4.0 PUSHED state, engine HEAD `a6a4010`
2. **`docs/RELEASE_NOTES_v1.4.0.md`** — özellikle Section 6 (Outstanding/deferred listesi)
3. **`docs/PHASE_STATUS.md`** — son banner: v1.4-Cleanup-Batch COMPLETE
4. **`docs/OPEN_QUESTIONS.md`** — Q-V1.5-HOOK-SCRIPTS-MISSING-01 partial RESOLVED + diğer açık Q'lar
5. **THIS BRIEF** — full context (4 finding inventory + 3 phase plan + quality gates + cross-check protocol)
6. **`docs/audits/v1.4-rules-schemas-templates-2026-05-07.md`** — v1.4 audit baseline (cross-ref için)
7. **`scripts/discovery/cannibalization_transform.py:106-171`** — normalize_url duplicate kanıtı (K-01 baseline)
8. **`scripts/discovery/content_gaps_transform.py:76-79`** — `_normalize_dfs_response` IMPORT discipline ÖRNEĞİ ("IMPORT discipline NOT copy" yorumu)
9. **`scripts/discovery/competitive_analysis_transform.py:580` + `scripts/discovery/tech_audit_transform.py:161`** — `_normalize_dfs_response` duplicate kanıtı (O-03 baseline)
10. **`schemas/cross-sheet-invariants.json`** — 15 rule declared (F-01..F-15)
11. **`scripts/validation/validate_invariants.py:1160-1167`** — 20 rule implemented (F-01..F-22 minus F-06/F-07)
12. **`tests/scripts/`** + **`tests/state/`** — mevcut test yapısı (D-01 missing coverage tespiti için)
13. **`requirements.txt` + `requirements-lock.txt`** + **`.github/workflows/ci.yml:21`** — version drift baseline (O-04)
14. **`scripts/maintenance/fix_schema_id_format.py`** + **`scripts/excel/bootstrap_excel.py`** — orphan candidate kanıtı (D-02 + D-03)
15. **`docs/DECISIONS.md`** — 6126B (18B headroom; yeni ADR yazma ÖNERİLMEZ)

---

## 🔍 Findings Inventory (Komple liste — 17 madde)

### 🔴 KRİTİK (3)

| ID | Başlık | Etki | Süre tahmini |
|---|---|---|---|
| **K-01** | `normalize_url` 10 dosyada duplicate (~700 satır) | -620 satır + drift sıfır | 30-45 dk |
| **K-02** | `cross-sheet-invariants.json` schema 15 rule, kod 20 rule (F-16..F-22 schema'da yok) | Schema-first compliance | 20-30 dk |
| **K-03** | ✅ DONE 2026-05-07 commit `a6a4010` (plugin.json + marketplace.json 1.3→1.4) | — | — |

### 🟡 ORTA (5)

| ID | Başlık | Etki | Süre tahmini |
|---|---|---|---|
| **O-01** | ✅ DONE 2026-05-07 commit `a6a4010` ("4 hook" → "6 hook") | — | — |
| **O-02** | ✅ DONE 2026-05-07 commit `a6a4010` (monitoring-weekly phantom path) | — | — |
| **O-03** | `_normalize_dfs_response` tech_audit + competitive_analysis duplicate (~100 satır) | -100 satır + IMPORT discipline | 15-20 dk |
| **O-04** | `requirements.txt` floor-version vs `requirements-lock.txt` pinned vs CI matrix Python 3.10 vs `.pyc` cpython-314 inconsistency | Reproducibility | 15-20 dk |
| **O-05** | F-23..F-28 memory referansları kod tabanında (`validate_invariants.py` + `cross-sheet-invariants.json`) drift gösteriyor — memory advance | Memory hygiene | 10-15 dk |

### 🟢 DÜŞÜK (5)

| ID | Başlık | Aksiyon | Süre tahmini |
|---|---|---|---|
| **D-01** | Missing test coverage spots: `migrate_legacy_events.py` direkt test yok; `migration 0001/0002` direkt test yok | TDD coverage gap closure | 30-45 dk |
| **D-02** | `scripts/maintenance/fix_schema_id_format.py` orphan (one-shot, no skill/command/test ref) | Archival decision: header DEPRECATED note ya da `scripts/_archive/` move | 5-10 dk |
| **D-03** | `scripts/excel/bootstrap_excel.py` orphan from skills/commands (test yok) | Document workflow: `rules/excel-discipline.md` "Run after schema change" notu | 5-10 dk |
| **D-04** | bash 3.2 macOS portability `check_append_only.sh` — already fine, no action | — | — |
| **D-05** | `requirements-lock.txt` `Python: 3.x` jenerik | `Python: 3.14` (cache evidence) güncelle | 2-5 dk |

### 🚀 Yeni Script Önerileri (Y-01..Y-07)

| ID | Script | Fonksiyon | Süre tahmini |
|---|---|---|---|
| **Y-01** | `scripts/util/url_normalize.py` | K-01 dedup target | (K-01 ile bundled) |
| **Y-02** | `scripts/util/dfs_response.py` | `_normalize_dfs_response` + `_safe_int/float/str` consolidate | 20-30 dk |
| **Y-03** | `scripts/hooks/check_naming.py` | Phase 13 placeholder, slug regex pre-commit | 60-90 dk **— AYRI BRIEF** |
| **Y-04** | `scripts/hooks/validate_before_write.py` | Phase 13 placeholder, schema PR enforcement | 60-90 dk **— AYRI BRIEF** |
| **Y-05** | `scripts/release/version_bump.py` | ADR-036 5-file sync otomasyon | 45-60 dk |
| **Y-06** | `scripts/util/profile_aware_defaults.py` | Profile cascade SSOT | 30-45 dk **— AYRI BRIEF** |
| **Y-07** | `scripts/state/dump_workspace.py` | Manager session state summary tek komut | 30-45 dk |

---

## 📦 Phase Plan (3 atomic milestone + 3 deferred)

### v1.5-Phase-1: URL + DFS Dedup Batch (S1)
**Scope:** K-01 + O-03 + Y-02
**Süre:** ~90-120 dk
**Risk:** DÜŞÜK (refactor + 970 test guard)
**Atomic commits:** ~3-4 (Y-01 extract + Y-02 extract + 10-file import migration + closeout)

### v1.5-Phase-2: Schema + Governance Batch (S2)
**Scope:** K-02 + D-01 + O-04 + O-05 + D-05
**Süre:** ~90-120 dk
**Risk:** ORTA (test ekleme + schema add + memory edit)
**Atomic commits:** ~4-5 (schema sync + missing tests + CI sync + memory + closeout)

### v1.5-Phase-3: Tooling Batch (S3)
**Scope:** Y-05 + Y-07 + D-02 + D-03
**Süre:** ~120-180 dk
**Risk:** ORTA (yeni script tasarımı, API kontratları)
**Atomic commits:** ~3-4 (version_bump + dump_workspace + archival decisions + closeout)

### Deferred (3 ayrı brief authoring + ayrı session gerektirir)

**v1.5-Phase-4: Y-03 + Y-04 Phase 13 Hooks**
- `check_naming.py` + `validate_before_write.py` rule body explicitly "Phase 13'te otomatize" qualifier
- Karar gerek: Phase 13 timeline finalize mi, yoksa qualifier preserve mi?
- Brief authoring + Süleyman onay turu zorunlu

**v1.5-Phase-5: Y-06 profile_aware_defaults**
- 5+ skill ortak kullanım → API kontratı tasarımı gerek
- Mevcut inline implementation'ları (discovery transforms) extract etmeden önce design discussion

**v1.5-Phase-6 (or v1.6+): H-E events.schema event_type enum bump 1.0 → 1.1**
- Q-V1.4-AUDIT-HIGH-01 Option b deferred
- Major schema_version bump (ADR-018 paterni)
- Migration script `scripts/migrations/0004_events_1_0_to_1_1.py` gerek
- 16 skill `event_type=manual + note=[skill=X]` workaround DSL deprecate
- Brief authoring + onay turu zorunlu

---

## 🛠️ Per-Phase Detailed Plan

### Phase 1: URL + DFS Dedup (Tier breakdown)

**Tier 1 — `scripts/util/url_normalize.py` extract (K-01 + Y-01, ~30-45 dk):**
1. Yeni dizin oluştur: `scripts/util/__init__.py` (empty namespace package)
2. Yeni dosya: `scripts/util/url_normalize.py`
   - Tek `normalize_url(url: str) -> str` fonksiyonu
   - D-03 invariant exact replication (8 rule: trim + lowercase scheme/host + IDN punycode + strip default ports + trailing slash + drop fragment + drop tracking params + sort query)
   - `_DEFAULT_PORTS` + `_TRACKING_PARAMS` constants
   - `URLNormalizeError(ValueError)` typed exception
   - `__all__` export list
3. **TDD:** önce `tests/util/test_url_normalize.py` (NEW) — 15+ test case (idempotency, IDN, port stripping, fragment drop, tracking param removal, edge cases empty/no-scheme/non-string)
4. Test red doğrula (file yokken)
5. `scripts/util/url_normalize.py` yaz (TDD green)
6. **Migration**: 10 dosyada `normalize_url` veya `_normalize_url` lokal tanımı sil, import ekle:
   - `scripts/ingestion/gsc_pull.py:97-171` → `from scripts.util.url_normalize import normalize_url as _normalize_url_d03; normalize_url = _normalize_url_d03`
     (alias gerekirse mevcut error class isimleri korunur)
   - `scripts/discovery/quickwins_transform.py:96-168`
   - `scripts/discovery/cannibalization_transform.py:106-171`
   - `scripts/discovery/content_decay_transform.py:106-172`
   - `scripts/discovery/on_page_audit_transform.py:108-173` (`_normalize_url`)
   - `scripts/discovery/schema_audit_transform.py:160-220` (`_normalize_url`)
   - `scripts/discovery/competitive_analysis_transform.py:191-255`
   - `scripts/discovery/geo_analysis_transform.py:201-279` (`_normalize_url`)
   - `scripts/planning/internal_links_transform.py:211-283`
   - `scripts/validation/validate_invariants.py:212-246` (`_normalize_url`)
7. **DİKKAT:** Her dosyanın kendi error class'ı var (`CannibalizationError`, `ContentDecayError`, vs). Lokal `normalize_url` dosya-spesifik error fırlatıyor — extract'tan sonra `URLNormalizeError`'u catch edip dosya-spesifik error'a wrap eden adapter pattern gerekebilir. Verify: `grep -nE "raise.*Error" <her dosya>` — error semantic preservation kritik.
8. pytest 970 → 970+ PASS verify (regression sıfır)
9. Atomic commit: `refactor(util): scripts/util/url_normalize.py extract — 10-file dedup ~620 lines (K-01 v1.5-Phase-1 Tier 1)`

**Tier 2 — `scripts/util/dfs_response.py` extract (Y-02 + O-03, ~30-45 dk):**
1. `scripts/util/dfs_response.py` — `_normalize_dfs_response` + `_safe_int` + `_safe_float` + `_safe_str` shared helpers
2. **TDD:** `tests/util/test_dfs_response.py` (NEW) — REST envelope + flat wrapper + edge cases
3. Migration: tech_audit + competitive_analysis (2 dosya) lokal kopyaları sil, import et
4. pytest 970+ PASS verify
5. Atomic commit: `refactor(util): scripts/util/dfs_response.py extract — tech_audit+competitive_analysis import discipline (O-03 + Y-02 v1.5-Phase-1 Tier 2)`

**Tier 3 — Closeout (~10-15 dk):**
1. Memory advance: `memory/project_current_status.md` v1.5-Phase-1 COMPLETE entry
2. PHASE_STATUS.md banner advance
3. RELEASE_NOTES_v1.4.0.md NO change (yeni release notes v1.5 final commit'te eklenir)
4. Atomic commit: `docs(closeout): v1.5-phase-1 COMPLETE — K-01 + O-03 + Y-01 + Y-02 RESOLVED + memory advance`

**Phase 1 Success Criteria:**
- [ ] -620 satır kod silindi (10 dosya)
- [ ] 0 grep hit `def normalize_url\|def _normalize_url` outside `scripts/util/url_normalize.py` + `scripts/state/env.py` + `validate_invariants.py` (eğer aliased)
- [ ] pytest 970+ PASS + 11 skip baseline (regression sıfır)
- [ ] tests/util/ dizinindeki yeni test'ler PASS
- [ ] D-03 idempotency self-check her transform'da hala çalışıyor
- [ ] F-10 (drift-check rule) hala doğru evaluate ediyor

### Phase 2: Schema + Governance (Tier breakdown)

**Tier 1 — `cross-sheet-invariants.json` F-16..F-22 sync (K-02, ~20-30 dk):**
1. `validate_invariants.py` docstring'lerden F-16..F-22 metadata çıkar (id + severity + category + rule body verbatim)
2. `schemas/cross-sheet-invariants.json` `rules[]` array'e 7 entry append (additive, schema_version unchanged)
3. **TDD:** `tests/schemas/test_cross_sheet_invariants_sync.py` (NEW) — registry consistency check (cross-sheet-invariants.json rule IDs ⊆ validate_invariants.py function names)
4. Hooks ya da CI step: registry sync invariant
5. pytest 970+ PASS verify
6. Atomic commit: `feat(schemas): cross-sheet-invariants.json F-16..F-22 sync — 15→22 rule registry coverage (K-02 v1.5-Phase-2 Tier 1)`

**Tier 2 — Missing test coverage (D-01, ~30-45 dk):**
1. `tests/state/test_migrate_legacy_events.py` (NEW) — migration script direct test (synthetic events.jsonl + jsonschema fail row + audit report verify)
2. `tests/scripts/test_migration_0001.py` (NEW) — schema_version 1.0→1.1 migration cases
3. `tests/scripts/test_migration_0002.py` (NEW) — schema_version 1.1→1.2 migration cases
4. pytest 970+ → 985+ PASS (regression sıfır + ~15 yeni test)
5. Atomic commit: `test(state+scripts): migration coverage — migrate_legacy + 0001 + 0002 (D-01 v1.5-Phase-2 Tier 2)`

**Tier 3 — CI Python sync (O-04 + D-05, ~15-20 dk):**
1. `.github/workflows/ci.yml:21` — `pip install jsonschema pytest openpyxl pyyaml` → `pip install -r requirements-lock.txt`
2. `.github/workflows/ci.yml` matrix 3.10 → 3.10 + 3.14 dual (ya da tek 3.12 modern compromise — Süleyman tercih)
3. `requirements-lock.txt` Python: 3.x → Python: 3.14 comment update
4. **TDD:** `tests/ci/test_python_version_consistency.py` (NEW) — matrix vs lock vs requirements parity
5. Atomic commit: `ci: python version + requirements-lock sync — matrix dual + lock-driven install (O-04 + D-05 v1.5-Phase-2 Tier 3)`

**Tier 4 — Memory hygiene (O-05, ~10-15 dk):**
1. `memory/project_current_status.md` — F-23..F-28 referanslarını "drift-check skill F-23..F-28 (governance helper)" olarak netleştir (validate_invariants.py F-22'ye kadar gider, F-23..F-28 drift-check skill body'sinde Engine Self-Governance subsection'da YA DA `tests/schemas/test_self_validate.py` + `tests/schemas/test_instance_validation.py` + `tests/hooks/test_hook_scripts_exist.py` cluster'da)
2. Cross-ref runtime grep verify F-23..F-28 nerede tanımlı
3. Memory advance commit: `docs(memory): F-23..F-28 reconcile — drift-check governance helper cluster (O-05 v1.5-Phase-2 Tier 4)`

**Tier 5 — Closeout (~10-15 dk):**
- Atomic commit: `docs(closeout): v1.5-phase-2 COMPLETE — K-02 + D-01 + O-04 + O-05 + D-05 RESOLVED + memory advance`

**Phase 2 Success Criteria:**
- [ ] cross-sheet-invariants.json 15 → 22 rule entry (additive)
- [ ] tests/state/test_migrate_legacy_events.py + tests/scripts/test_migration_000{1,2}.py NEW
- [ ] CI matrix Python version + requirements-lock-driven install
- [ ] Memory F-23..F-28 reconciled
- [ ] pytest 985+ PASS + 11 skip

### Phase 3: Tooling Batch (Tier breakdown)

**Tier 1 — `scripts/release/version_bump.py` (Y-05, ~45-60 dk):**
1. CLI: `version_bump.py --to <semver> [--dry-run]`
2. Logic:
   - plugin.json `version` update
   - marketplace.json `metadata.version` + `plugins[0].description` semver prefix update
   - README.md `**Status:**` banner version update
   - INSTALL.md `Status:` banner version update
   - Verify `docs/RELEASE_NOTES_v<semver>.md` exists (else WARN, no auto-create — manual authoring required)
3. **TDD:** `tests/scripts/test_version_bump.py` (NEW) — golden file fixtures + dry-run + apply mode
4. Atomic commit: `feat(release): scripts/release/version_bump.py — ADR-036 5-file sync automation (Y-05 v1.5-Phase-3 Tier 1)`

**Tier 2 — `scripts/state/dump_workspace.py` (Y-07, ~30-45 dk):**
1. CLI: `dump_workspace.py [--project <slug>]`
2. Output (stderr): manager session state summary
   - events.jsonl son 5 satır
   - master.xlsx#master_task TODO count
   - workflow_runner pending approvals
   - _state/backups son 7 timestamp
   - drift-check verdict (consistency-report.json son)
3. **TDD:** `tests/scripts/test_dump_workspace.py` (NEW) — synthetic workspace fixtures
4. Atomic commit: `feat(state): scripts/state/dump_workspace.py — manager session state summary tek komut (Y-07 v1.5-Phase-3 Tier 2)`

**Tier 3 — Orphan archival decisions (D-02 + D-03, ~10-15 dk):**
1. `scripts/maintenance/fix_schema_id_format.py` — file header'a `# DEPRECATED — once-applied 2026-05-07 (Q-V1.4-AUDIT-CRITICAL-01 K-02 closure). Kept for audit history per ADR-012.` notu ekle
2. `rules/excel-discipline.md` — yeni section: "## When to run `scripts/excel/bootstrap_excel.py`" — schema bump sonrası manuel re-generate workflow
3. Atomic commit: `docs: orphan tooling archival notes — fix_schema_id_format DEPRECATED + bootstrap_excel workflow doc (D-02 + D-03 v1.5-Phase-3 Tier 3)`

**Tier 4 — Closeout (~15 dk):**
- v1.5 final RELEASE_NOTES yazma DEĞİL bu phase'de — v1.5 milestone tamamlanması Phase 4-6 (deferred) ile birlikte. Bu phase'in closeout'u sadece interim memory advance.
- Atomic commit: `docs(closeout): v1.5-phase-3 COMPLETE — Y-05 + Y-07 + D-02 + D-03 RESOLVED + memory advance`

**Phase 3 Success Criteria:**
- [ ] `scripts/release/version_bump.py` + tests
- [ ] `scripts/state/dump_workspace.py` + tests
- [ ] `fix_schema_id_format.py` DEPRECATED note
- [ ] `rules/excel-discipline.md` bootstrap_excel workflow doc
- [ ] pytest 990+ PASS + 11 skip

---

## 🚦 Quality Gates (Non-Negotiable)

### Per-Task TDD Cycle
1. **Test yaz (RED)** — test FAIL etmeli (henüz implementation yok)
2. **Minimal implementation (GREEN)** — test PASS etmeli
3. **Refactor (IMPROVE)** — pattern temizliği
4. **pytest 970+ PASS verify** — regression sıfır

### Per-Phase Commit Pattern
- 1 atomic commit per Tier (yukarıdaki Per-Phase plan'da listelendi)
- Closeout commit zorunlu (memory advance + banner update)
- Commit message format: `<type>(<scope>): <body> (<finding-id> <phase-id> Tier <n>)`
- Type: `refactor` / `feat` / `test` / `fix` / `docs` / `chore` / `ci`
- Co-Authored-By footer zorunlu

### Cross-Check Protocol (Lesson 38 v2 + Lesson 67 Stacked, 30+ ardışık kanıt)

Her Tier'da brief execute etmeden ÖNCE:
1. **Brief premise verify** — "iddia X" runtime grep ile doğrula. Brief premise revize OLABİLİR (memory: "brief premise revize 5x v1.4-deep-audit-fix").
2. **File existence** — claim edilen dosyalar gerçekten var mı? `find` + `grep -l` ile cross-check.
3. **Test infrastructure** — yeni test'in kullanacağı fixture/conftest var mı?

Her Tier'da implementation SIRASINDA:
4. **Manager pre-dispatch full-file inspect** — agent dispatch öncesi dosyanın bütünü read'le, satır numaraları ile çalış.
5. **Self-correction** — claim → runtime verify → claim doğru mu yanlış mı? Yanlışsa brief premise revize.

Her Tier KAPANMADAN ÖNCE:
6. **Runtime grep verify** — fix sonrası grep ile pattern eliminated mi (örn. K-01 sonrası `def normalize_url` 0 hit outside `scripts/util/`).
7. **pytest 970+ PASS** — regression sıfır.
8. **DURUR conditions** verify — listede sayılan tüm invariantlar intact.

### DURUR (no fall-back) Conditions

**Phase boyunca STOP koşulları:**
- pytest baseline regression (970 → 969 ya da daha az PASS) → STOP, root cause analyze, fix
- `.mcp.json` byte change → STOP (F-16 invariant breach)
- DECISIONS.md cap aşımı (>6144B) → STOP, ADR consolidation
- plugin.json version drift (1.4.0 unchanged in this milestone) → STOP, version drift breach
- Slug literal grep hit → STOP (plugin agnostik breach)
- Schema validation FAIL (`Draft7Validator.check_schema`) → STOP, schema authority bozulamaz

**Cross-check FAIL koşulları:**
- Brief premise yanlış → revize, NOT bypass
- Runtime grep "yapıldı" iddiasını çürütüyor → STOP, fix
- Memory'deki claim runtime'da false → memory advance, claim correct

### Memory Advance Protocol

Her phase-end'de:
1. `memory/project_current_status.md` — phase COMPLETE entry, engine HEAD hash
2. `memory/project_phase_lessons.md` — yeni Lesson 38 v2 catch (cumulative count update)
3. `memory/MEMORY.md` (index) — phase listesi güncel
4. PHASE_STATUS.md banner advance

---

## 🔄 Cross-Reference Map

### Findings ↔ Files
- K-01 → 10 transform files + `validate_invariants.py:212`
- K-02 → `cross-sheet-invariants.json:15-145` + `validate_invariants.py:1160-1167`
- O-03 → `competitive_analysis_transform.py:580` + `tech_audit_transform.py:161`
- O-04 → `requirements.txt` + `requirements-lock.txt` + `.github/workflows/ci.yml:21`
- O-05 → `memory/MEMORY.md` + `memory/project_current_status.md` + `validate_invariants.py` + drift-check skill
- D-01 → `tests/state/` + `tests/scripts/`
- D-02 → `scripts/maintenance/fix_schema_id_format.py`
- D-03 → `scripts/excel/bootstrap_excel.py` + `rules/excel-discipline.md`
- D-05 → `requirements-lock.txt:2`
- Y-01 → `scripts/util/url_normalize.py` (NEW)
- Y-02 → `scripts/util/dfs_response.py` (NEW)
- Y-05 → `scripts/release/version_bump.py` (NEW)
- Y-07 → `scripts/state/dump_workspace.py` (NEW)

### ADR References
- ADR-009 — master-excel schema-driven (template reference for util extraction discipline)
- ADR-012 — HTTP $id authority
- ADR-018 — Schema additive bump (cross-sheet-invariants.json sync K-02)
- ADR-021 — events.jsonl path canonical
- ADR-026 — DECISIONS.md 6144B cap
- ADR-035 — PSEO_WORKSPACE_ROOT canonical
- ADR-036 — 5-file version sync invariant (Y-05 automate target)
- ADR-038 — R-XX numbering gap-tolerant (template for F-NN gap-tolerant if F-XX numbering ever extends)

---

## ✅ Master Success Criteria (v1.5-Phase 1+2+3 cumulative)

- [ ] **10 finding RESOLVED** (K-01 + K-02 + O-03 + O-04 + O-05 + D-01 + D-02 + D-03 + D-05; K-03/O-01/O-02 already done in `a6a4010`)
- [ ] **4 yeni script authored** (Y-01 + Y-02 + Y-05 + Y-07)
- [ ] **3 phase deferred to ayrı brief'ler** (Y-03+Y-04 / Y-06 / H-E)
- [ ] **pytest 970 → 990+ PASS + 11 skip** (regression sıfır + ~20 yeni test)
- [ ] **`.mcp.json` 482B byte-byte korundu** (F-16 30+ commit cumulative → 40+ commit cumulative)
- [ ] **Plugin agnostik 0 slug literal** (F-16 grep zero hits)
- [ ] **DECISIONS.md ≤6144B** (yeni ADR yazılmadıysa 6126B unchanged ÖNERİLİR)
- [ ] **plugin.json 1.4.0 unchanged** (v1.5 milestone bu brief'in scope'u DEĞİL — final v1.5 release commit ayrı session'da)
- [ ] **3 closeout commit** + memory advance + PHASE_STATUS banner per phase
- [ ] **Atomic phase paterni 52 → 55+ kanıt** (3 yeni phase consecutive convergent)
- [ ] **Cross-Audit Metodoloji** Lesson 38 v2 cumulative count update (30 → 33+)

---

## 🚀 Kickoff Strategy

**Önerilen sıra:** Phase 1 → Phase 2 → Phase 3 (sequential, ayrı session'lar).

**Her session başlangıcı:**
1. Wakeup sequence oku (15 madde)
2. THIS BRIEF Section "Phase X" oku
3. Tier 1'den başla, sıralı git
4. Her Tier'da TDD red-green + cross-check + atomic commit
5. Phase-end closeout commit
6. Push to origin/main (engine repo PRIVATE)

**Her session sonu:**
1. Memory advance
2. PHASE_STATUS banner advance
3. Atomic phase paterni cumulative count update

**Final v1.5 milestone closeout (Phase 3 sonrası, opsiyonel):**
- v1.5 final release commit (plugin.json + marketplace.json 1.4.0 → 1.5.0 + RELEASE_NOTES_v1.5.0.md NEW + README/INSTALL banner update)
- Bu brief'in scope'u DEĞİL — Phase 1+2+3 tamamlanınca **ayrı v1.5-marketplace-publication-wave-1** session'ı.

---

## 📚 Cross-References

- **Audit baseline:** 2026-05-07 scripts audit conversation (Claude Code session, not committed) — bu brief findings inventory'i o audit'ten türetilmiştir
- **Predecessor briefs (paterni reuse):**
  - `docs/superpowers/plans/v1.4-bootstrap-paths-fix-brief.md` — Tier breakdown paterni
  - `docs/superpowers/plans/v1.4-deep-audit-fix-brief.md` — multi-Tier brief paterni
  - `docs/superpowers/plans/v1.4-deep-audit-fix-resume-prompts.md` — fresh-session resume prompt paterni
- **Test infrastructure references:**
  - `tests/scripts/conftest.py` — fixture conventions
  - `tests/state/test_events_schema_compliance.py` — schema instance test paterni
- **Spec references:**
  - `rules/skills.md` — Skill body discipline (multi-block format)
  - `rules/append-only-state.md` — events.jsonl + workflows JSON discipline
  - `rules/schema-first.md` — Schema PR > implementation PR sırası
  - `rules/single-source-of-truth.md` — workspace path convention authority
  - `rules/excel-discipline.md` — master.xlsx writer policy

---

**Brief authoring complete. Fresh session kickoff prompt: see chat-paste prompt at end of `2026-05-07` audit session (Claude Code). Engine HEAD baseline: `a6a4010` (v1.4.0 marketplace publication wave 1, PUSHED).**
