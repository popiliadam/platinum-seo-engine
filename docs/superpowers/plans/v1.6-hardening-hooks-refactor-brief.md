# Manager Session Brief — v1.6 Hardening + Hooks + Refactor (post-v1.5.0)

**Yazım tarihi:** 2026-05-07 (post-v1.5.0 milestone CLOSED, engine HEAD `2479a94` PUSHED + git tag `v1.5.0` annotated)
**Manager session role:** v1.5 milestone post-close kalan 3 backlog item kümesini 3 atomic milestone phase altında çöz. Quality discipline non-negotiable: TDD red-green her task için + Lesson 38 v2 cross-check + atomic commit per phase + memory advance per milestone-end + runtime grep verify her wave-end.
**Estimated total duration:** ~9-12 saat (3 phase × 90-240 dk + closeouts) — **3 ayrı fresh session önerilir**, 1 tek session'a sıkıştırma YASAK (kalite kaybı garantili; 9 saat tek session = context pollution + Lesson 38 v2 cross-check kalitesi düşer).

---

## 🎯 Hedef

v1.5.0 milestone CLOSED post-state'inde kalan 3 backlog cluster'ını çözmek:

1. **v1.6-Phase-1 — Phase 13 Hook Authoring:** Y-03 + Y-04 + Q-V1.5-HOOK-SCRIPTS-MISSING-01 remaining 2 (4-of-4 RESOLVED final)
2. **v1.6-Phase-2 — Telemetri Kalite (events.schema enum bump):** H-E events.schema event_type enum 1.0 → 1.1 + scripts/migrations/0004 + 16 skill DSL deprecate
3. **v1.6-Phase-3 — Tech Debt Refactor:** O-03 + Y-02 (DataForSEO response semantic-aware refactor) + Y-06 (profile_aware_defaults SSOT)

**Sınır:** v1.6 milestone scope **bu brief**. Wave 3+4 marketplace publication PUBLIC + Süleyman ek items ayrı brief'ler (engine repo PRIVATE→PUBLIC transition kritik karar).

**KRİTİK invariantlar (her phase boyunca):**
- pytest baseline 1066 PASS + 10 SKIP — **regression sıfır zorunlu**
- `.mcp.json` 482B byte-byte korunma F-16 invariant — 37+ commit cumulative
- ADR-036 5-file sync 1.5.0 invariant intact (yeni release notes/version bump v1.6'ya kadar YASAK; bu brief'ler v1.5.0 baseline'ında çalışır, plugin.json bump v1.6 final commit'inde olur)
- DECISIONS.md 6126/6144B — 18B headroom; yeni ADR yazılmaması ÖNERİLİR (mevcut ADR-018/036/038 paterni reuse)
- Plugin agnostik discipline: F-16 invariant — 0 slug literal in runtime code

---

## 📋 Wakeup Sequence (zorunlu okuma sırası)

1. **`memory/MEMORY.md`** + **`memory/project_current_status.md`** — v1.5.0 MILESTONE CLOSED state, engine HEAD `2479a94`
2. **`docs/RELEASE_NOTES_v1.5.0.md`** — v1.5 cumulative kapsam + drift baseline
3. **`docs/PHASE_STATUS.md`** — son banner: v1.5.0 MILESTONE CLOSED
4. **`docs/OPEN_QUESTIONS.md`** — Q-V1.5-HOOK-SCRIPTS-MISSING-01 partial-2-of-4 state (Phase 1 hedef: RESOLVED final)
5. **THIS BRIEF** — full context (3 cluster inventory + 3 phase plan + quality gates + cross-check protocol)
6. **`docs/superpowers/plans/v1.5-audit-closure-brief.md`** — predecessor paterni reuse (Tier breakdown + DURUR conditions)
7. **`schemas/events.schema.json`** — Phase 2 baseline (mevcut event_type enum)
8. **`scripts/state/migrate_legacy_events.py`** + **`scripts/migrations/0001..0003`** — Phase 2 migration paterni reuse baseline
9. **`scripts/discovery/tech_audit_transform.py:161`** + **`scripts/discovery/competitive_analysis_transform.py:519`** — Phase 3 O-03 baseline (Lesson 38 v2 #34-35 semantic divergence cite)
10. **`scripts/discovery/quickwins_transform.py`** + 4-5 diğer transform — Phase 3 Y-06 inline default config'leri (extract candidate)
11. **`tests/hooks/test_hook_scripts_exist.py`** — Phase 1 EXPECTED_DEFERRED set update target ({check_naming, validate_before_write} → {})
12. **`rules/naming.md`** — Phase 1 Y-03 check_naming.py rule body authority
13. **`rules/schema-first.md`** — Phase 1 Y-04 validate_before_write.py rule body authority
14. **`scripts/release/version_bump.py`** + **`scripts/state/dump_workspace.py`** — Phase 1+2+3 paterni reuse (own-tooling)
15. **`docs/DECISIONS.md`** — 6126B (18B headroom; yeni ADR yazma ÖNERİLMEZ)

---

## 🔍 Findings Inventory

### v1.6-Phase-1: Y-03 + Y-04 + Q-V1.5-HOOK-SCRIPTS-MISSING-01 remaining 2

| ID | Başlık | Etki | Süre tahmini |
|---|---|---|---|
| **Y-03** | `scripts/hooks/check_naming.py` — pre-commit slug regex gate | Yanlış case/suffix/slug naming hatası commit-sırası catch | 30-45 dk |
| **Y-04** | `scripts/hooks/validate_before_write.py` — schema-first PR enforcement | "Önce schema, sonra kod" rule violations commit-sırası catch | 30-45 dk |
| **Q-V1.5-HOOK-SCRIPTS-MISSING-01 remaining 2** | check_naming.py + validate_before_write.py = Y-03 + Y-04 (aynı) | 4-of-4 hook authored = Q RESOLVED final | (Y-03+Y-04 ile birlikte) |

**Phase 1 Total Tahmini:** ~90-120 dk

**Strategic context (Süleyman karar):**
- Phase 13 PostgreSQL migration timeline'ı bu hook'ları blokemez — hook'lar bağımsız (naming + schema-first kuralları database transition'dan etkilenmez)
- "Phase 13'te otomatize" qualifier preserve kararı v1.5 milestone'da partial-2-of-4 RESOLVED idi; v1.6'da remaining 2 finalize → Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final (4-of-4)

### v1.6-Phase-2: H-E events.schema enum bump

| ID | Başlık | Etki | Süre tahmini |
|---|---|---|---|
| **H-E** | `events.schema.json` event_type enum 1.0 → 1.1 (16 skill canonical event_type ekle) | Audit log telemetri doğruluk + workaround DSL deprecate | 30-45 dk |
| **scripts/migrations/0004** | Legacy `event_type=manual + note=[skill=X]` → canonical `event_type=skill_X` | Geçmiş events.jsonl backwards-compat | 45-60 dk |
| **16 skill DSL deprecate** | 16 skill body update (manual + note workaround → canonical event_type) | Skill body update + smoke test | 30-45 dk |
| **Tests + ADR cite** | events.schema 1.1 compliance + ADR-018 paterni cite | Schema versioning discipline | 10-15 dk |

**Phase 2 Total Tahmini:** ~120-180 dk

**Schema authority:**
- ADR-018 paterni reuse: schema_version 1.0 → 1.1 (additive enum extension; major bump çünkü migration gerek)
- Migration paterni 0001/0002/0003 reuse (importlib.util.spec_from_file_location numeric-prefix paterni)

### v1.6-Phase-3: O-03 + Y-02 + Y-06

| ID | Başlık | Etki | Süre tahmini |
|---|---|---|---|
| **Y-02** | `scripts/util/dfs_response.py` NEW — endpoint-aware DFS response normalize | tech_audit + competitive_analysis dedup + future-proof | 45-60 dk |
| **O-03** | tech_audit + competitive_analysis migrate to scripts.util.dfs_response | -100 satır + IMPORT discipline | 30-45 dk |
| **Y-06** | `scripts/util/profile_aware_defaults.py` NEW + 5+ skill migrate | Profile cascade SSOT + skill body kısalma | 60-90 dk |

**Phase 3 Total Tahmini:** ~180-240 dk

**Semantic divergence context (Lesson 38 v2 #34-35 v1.5-Phase-1):**
- tech_audit `_normalize_dfs_response`: Lighthouse + page_metrics + audits handle (web sayfa performans)
- competitive_analysis `_local_normalize_dfs`: keyword response fallback (import-isolation by-design)
- Phase 3 Y-02 endpoint-aware dispatcher: response shape detection → doğru normalize call

---

## 📦 Phase Plan (3 atomic milestone)

### v1.6-Phase-1: Phase 13 Hook Authoring (S1)
**Scope:** Y-03 + Y-04 + Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final
**Süre:** ~90-120 dk
**Risk:** ORTA (yeni hook + .claude/settings.json hook config + EXPECTED_DEFERRED set 2→0 update)
**Atomic commits:** ~4 (check_naming + validate_before_write + hook config + closeout)

### v1.6-Phase-2: events.schema enum bump (S2)
**Scope:** H-E + scripts/migrations/0004 + 16 skill DSL deprecate
**Süre:** ~120-180 dk
**Risk:** ORTA-YÜKSEK (major schema_version bump + 16 skill body update + migration script)
**Atomic commits:** ~5 (schema bump + migration + 16 skill update + tests + closeout)

### v1.6-Phase-3: DataForSEO + Profile Refactor (S3)
**Scope:** O-03 + Y-02 + Y-06
**Süre:** ~180-240 dk
**Risk:** ORTA (semantic-aware refactor + 5+ skill profile API design discussion)
**Atomic commits:** ~4-5 (Y-02 dfs_response + O-03 migrate + Y-06 profile + 5+ skill migrate + closeout)

---

## 🛠️ Per-Phase Detailed Plan

### Phase 1: Hook Authoring (Tier breakdown)

**Tier 1 — `scripts/hooks/check_naming.py` + tests (Y-03, ~30-45 dk):**
1. **TDD:** `tests/hooks/test_check_naming.py` (NEW) — 8+ test:
   - `test_slug_regex_validates_lowercase_alphanumeric_dash`
   - `test_skill_folder_name_matches_skill_md_slug`
   - `test_command_md_slug_matches_filename`
   - `test_schema_filename_pattern_lowercase_dash_schema_json`
   - `test_master_xlsx_sheet_naming_lowercase_underscore`
   - `test_invalid_uppercase_in_slug_rejected`
   - `test_invalid_special_char_in_slug_rejected`
   - `test_no_violations_pass_exit_0`
   - `test_violation_exits_1_with_line_cite`
2. RED verify (check_naming.py yokken)
3. **GREEN:** `scripts/hooks/check_naming.py`:
   - Pre-commit context: `git diff --cached --name-only --diff-filter=AM`
   - File pattern recognition (skills/*/SKILL.md, commands/*.md, schemas/*.schema.json, etc)
   - Slug regex per file type (`rules/naming.md` rule body authority)
   - Exit 0 (pass) / Exit 1 (fail with line cite)
4. GREEN verify + regression (pytest 1066 → 1075+ PASS)
5. Atomic commit: `feat(hooks): scripts/hooks/check_naming.py — Phase 13 pre-commit naming gate (Y-03 v1.6-Phase-1 Tier 1)`

**Tier 2 — `scripts/hooks/validate_before_write.py` + tests (Y-04, ~30-45 dk):**
1. **TDD:** `tests/hooks/test_validate_before_write.py` (NEW) — 6+ test:
   - `test_schema_modified_with_kod_change_passes`
   - `test_kod_modified_without_schema_change_when_schema_referenced_rejects`
   - `test_schema_only_modified_passes`
   - `test_kod_only_when_no_schema_reference_passes`
   - `test_test_only_modified_passes`
   - `test_doc_only_modified_passes`
2. RED verify
3. **GREEN:** `scripts/hooks/validate_before_write.py`:
   - "kod" file type detection (transforms, scripts/state, scripts/excel, scripts/discovery, scripts/planning, scripts/validation)
   - Schema reference detection (grep `schemas/<slug>.schema.json` in kod files)
   - Cross-check: schema files modified for those references in same commit
   - `rules/schema-first.md` rule body authority
4. GREEN verify
5. Atomic commit: `feat(hooks): scripts/hooks/validate_before_write.py — Phase 13 schema-first enforcement gate (Y-04 v1.6-Phase-1 Tier 2)`

**Tier 3 — Hook config + EXPECTED_DEFERRED set update (~10-15 dk):**
1. `.claude/settings.json` ya da `.claude-plugin/hooks/pre-commit.json`: 4 hook entry registration (check_append_only.sh + check_excel_writer.py + check_naming.py + validate_before_write.py) — runtime path discovery sırasında authority
2. `tests/hooks/test_hook_scripts_exist.py`: EXPECTED_DEFERRED set update {check_naming, validate_before_write} → {} empty (Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final)
3. `docs/OPEN_QUESTIONS.md`: Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final (was partial-2-of-4)
4. Atomic commit: `feat(hooks)+test+docs: 4 hook activation complete + Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final (v1.6-Phase-1 Tier 3)`

**Tier 4 — Closeout (~15 dk):**
1. `memory/project_current_status.md` v1.6-Phase-1 COMPLETE entry
2. PHASE_STATUS.md banner advance v1.5.0 MILESTONE CLOSED → v1.6-Phase-1 COMPLETE
3. Atomic commit: `docs(closeout): v1.6-phase-1 (Hook Authoring) COMPLETE — Y-03 + Y-04 + Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final (4-of-4)`

**Phase 1 Success Criteria:**
- [ ] `scripts/hooks/check_naming.py` NEW + 8+ test
- [ ] `scripts/hooks/validate_before_write.py` NEW + 6+ test
- [ ] Hook config registration (4-of-4 hooks active)
- [ ] `tests/hooks/test_hook_scripts_exist.py` EXPECTED_DEFERRED {} empty
- [ ] `docs/OPEN_QUESTIONS.md` Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final
- [ ] pytest 1066 → 1080+ PASS + 10 SKIP (regression sıfır + ~14 yeni test)
- [ ] `.mcp.json` 482B byte-byte korundu (F-16 38+ commit cumulative)
- [ ] DECISIONS.md ≤6144B (no new ADR — paterni reuse)
- [ ] plugin.json 1.5.0 unchanged
- [ ] Atomic phase paterni 58 → 59'uncu kanıt cumulative

---

### Phase 2: events.schema enum bump (Tier breakdown)

**Tier 1 — `schemas/events.schema.json` bump (H-E core, ~30-45 dk):**
1. Identify 16 skills using `event_type=manual + note=[skill=X]` workaround:
   - `grep -rn "event_type.*manual.*note.*skill" skills/ scripts/`
   - Cumulative skill list (TBD by runtime grep — brief premise revize candidate per Lesson 38 v2)
2. **TDD:** `tests/schemas/test_events_schema_event_type_enum_v1_1.py` (NEW) — 5+ test:
   - `test_canonical_event_types_for_16_skills_present` (skill_cannibalization + skill_quickwins + ...)
   - `test_legacy_event_types_preserved` (manual + import + export + system_init still valid)
   - `test_schema_version_const_1_1`
   - `test_event_type_enum_additive_no_breaking_removal`
3. RED verify (schema'da yeni enum yok)
4. **GREEN:** `schemas/events.schema.json`:
   - `schema_version` const "1.0" → "1.1"
   - `event_type` enum extension: 16 yeni `skill_<name>` entry + legacy preserved (additive bump)
5. GREEN verify
6. Atomic commit: `feat(schemas): events.schema.json event_type enum bump 1.0→1.1 — 16 skill canonical event_type additive (H-E v1.6-Phase-2 Tier 1)`

**Tier 2 — `scripts/migrations/0004_events_1_0_to_1_1.py` + tests (~45-60 dk):**
1. **TDD:** `tests/scripts/test_migration_0004.py` (NEW) — 9+ test (paterni 0001/0002/0003 reuse):
   - `test_legacy_manual_with_note_skill_x_migrates_to_skill_x`
   - `test_legacy_event_types_preserved_unchanged`
   - `test_unknown_skill_in_note_falls_back_to_manual_with_note`
   - `test_idempotent_running_twice`
   - `test_migrates_jsonl_atomically_temp_then_rename`
   - `test_migration_module_loadable_via_importlib_spec`
   - `test_schema_version_check_pre_post`
   - `test_partial_migration_state` (already-migrated rows preserved)
   - `test_migration_creates_audit_report`
2. RED verify
3. **GREEN:** `scripts/migrations/0004_events_1_0_to_1_1.py`:
   - `importlib.util.spec_from_file_location` paterni reuse (numeric-prefix Python identifier issue)
   - `migrate_event(event: dict) -> dict` per-event migration
   - `migrate_jsonl(path: Path) -> tuple[int, list[str]]` whole file migration with atomic temp+rename
   - Parse `note=[skill=X]` regex extract → set `event_type=skill_X`
   - Unknown skill name fallback: preserve `event_type=manual` + audit report entry
4. GREEN verify
5. Atomic commit: `feat(migrations): scripts/migrations/0004_events_1_0_to_1_1.py — note=[skill=X] DSL migration (H-E v1.6-Phase-2 Tier 2)`

**Tier 3 — 16 skill body update (~30-45 dk):**
1. Identified 16 skill list (Tier 1 grep) → per skill update event_type emission
2. **CAUTION:** Skill body update = SKILL.md frontmatter + body sections değişebilir; care for invariants:
   - F-16 (plugin agnostik) intact
   - skill body multi-block format preserve
   - rule R-XX cite intact (lesson 8 v6 paterni)
3. Smoke test per skill (existing `tests/skills/test_<skill>.py` veya integration test'ler)
4. Atomic commit: `refactor(skills): 16 skill event_type canonical migration — workaround DSL deprecate (H-E v1.6-Phase-2 Tier 3)`

**Tier 4 — Tests + ADR cite + invariants (~10-15 dk):**
1. `tests/state/test_events_schema_compliance.py` update for v1.1 (compliance testleri yeni enum'a tolerant + canonical kabul)
2. `docs/DECISIONS.md` ADR-018 cite update (additive paterni 7'inci uygulama? — runtime grep verify cumulative count)
3. Atomic commit: `test+docs: events.schema 1.1 compliance + ADR-018 cite (H-E v1.6-Phase-2 Tier 4)`

**Tier 5 — Closeout (~15 dk):**
- Memory advance + banner advance + push
- Atomic commit: `docs(closeout): v1.6-phase-2 (events.schema enum bump) COMPLETE — H-E + 16 skill DSL deprecate RESOLVED`

**Phase 2 Success Criteria:**
- [ ] `schemas/events.schema.json` schema_version 1.0 → 1.1 + event_type enum 16 yeni canonical entry (additive)
- [ ] `scripts/migrations/0004_events_1_0_to_1_1.py` NEW + 9+ test
- [ ] 16 skill body update canonical event_type emission (workaround DSL deprecate)
- [ ] `tests/state/test_events_schema_compliance.py` v1.1 compliance update
- [ ] pytest 1080+ → 1100+ PASS + 10 SKIP (regression sıfır + ~10 yeni migration test)
- [ ] `.mcp.json` 482B byte-byte korundu (F-16 39+ commit cumulative)
- [ ] DECISIONS.md ≤6144B (ADR-018 cite update only, no new ADR)
- [ ] plugin.json 1.5.0 unchanged
- [ ] Atomic phase paterni 59 → 60'ıncı kanıt cumulative

---

### Phase 3: O-03 + Y-02 + Y-06 (Tier breakdown)

**Tier 1 — `scripts/util/dfs_response.py` + tests (Y-02, ~45-60 dk):**
1. **TDD:** `tests/util/test_dfs_response.py` (NEW) — 10+ test:
   - `test_keyword_response_envelope_REST` (DFS keyword endpoint shape)
   - `test_keyword_response_flat_wrapper` (legacy fallback shape)
   - `test_lighthouse_response_envelope` (page_metrics + audits shape)
   - `test_safe_int_edge_cases` (None, "", "42", 42, 42.5)
   - `test_safe_float_edge_cases` (None, "", "3.14", 3.14, "abc")
   - `test_safe_str_edge_cases` (None, "", 42, "text")
   - `test_endpoint_dispatcher_keyword_detection`
   - `test_endpoint_dispatcher_lighthouse_detection`
   - `test_endpoint_dispatcher_unknown_falls_back`
   - `test_dfs_response_module_exports`
2. RED verify
3. **GREEN:** `scripts/util/dfs_response.py`:
   - `normalize_dfs_response(response, endpoint_type=None)` — endpoint-aware top-level dispatcher
   - `_normalize_keyword_response(response)` — REST envelope + flat wrapper handling
   - `_normalize_lighthouse_response(response)` — page_metrics + audits
   - `safe_int / safe_float / safe_str` — typed coercion helpers (None/empty → default)
   - `__all__` export list
   - `DFSResponseError(ValueError)` typed exception
4. GREEN verify
5. Atomic commit: `feat(util): scripts/util/dfs_response.py — endpoint-aware DFS response normalize (Y-02 v1.6-Phase-3 Tier 1)`

**Tier 2 — Migration tech_audit + competitive_analysis (O-03, ~30-45 dk):**
1. `scripts/discovery/tech_audit_transform.py:161` — lokal `_normalize_dfs_response` sil, `from scripts.util.dfs_response import normalize_dfs_response as _normalize_dfs_response` import
2. `scripts/discovery/competitive_analysis_transform.py:519` — lokal `_local_normalize_dfs` sil, fallback paterni preserve via try/except adapter
3. **CAUTION:** Lesson 38 v2 #34 fallback by-design — `_local_normalize_dfs` import-isolation fallback için ya bırak ya wrapper ile çağır. Karar TBD by Phase 3 manager session (semantic preservation kritik).
4. Existing tests (`test_tech_audit` + `test_competitive_analysis`) still pass
5. pytest 1100+ regression sıfır verify
6. Atomic commit: `refactor(transforms): tech_audit + competitive_analysis migrate to scripts/util/dfs_response (O-03 v1.6-Phase-3 Tier 2)`

**Tier 3 — Y-06 `scripts/util/profile_aware_defaults.py` + 5+ skill migrate (~60-90 dk):**
1. **Identify 5+ skills with inline profile defaults (runtime grep):**
   - `grep -rn "profile.*default\|inline default" scripts/discovery/ scripts/planning/`
   - Candidate cumulative list (Lesson 38 v2 brief premise revize candidate):
     - `scripts/discovery/quickwins_transform.py`
     - `scripts/discovery/content_decay_transform.py`
     - `scripts/discovery/cannibalization_transform.py`
     - `scripts/planning/internal_links_transform.py`
     - `scripts/discovery/schema_audit_transform.py`
     - `scripts/discovery/on_page_audit_transform.py`
     - `scripts/discovery/geo_analysis_transform.py`
2. **API design (manager session karar):**
   - `load_profile(workspace_root: Path, project_slug: str) -> dict` — read profile config
   - `cascade_defaults(profile: dict, override_args: dict) -> dict` — profile defaults + CLI override merge
   - `apply_skill_specific_overrides(merged: dict, skill: str) -> dict` — skill-specific cascade layer (optional)
3. **TDD:** `tests/util/test_profile_aware_defaults.py` (NEW) — 12+ test:
   - `test_load_profile_from_workspace_path`
   - `test_load_profile_missing_returns_empty`
   - `test_cascade_defaults_override_precedence` (CLI > profile > inline)
   - `test_cascade_defaults_missing_keys_use_inline`
   - `test_skill_specific_overrides`
   - `test_module_exports`
   - + edge cases (None args, empty profile, malformed JSON)
4. RED verify
5. **GREEN:** `scripts/util/profile_aware_defaults.py`
6. **Migration:** 5+ skills migrate to `from scripts.util.profile_aware_defaults import load_profile, cascade_defaults`
7. pytest regression sıfır verify
8. Atomic commit: `feat(util)+refactor(skills): scripts/util/profile_aware_defaults.py + 5+ skill migrate (Y-06 v1.6-Phase-3 Tier 3)`

**Tier 4 — Closeout (~15 dk):**
- Memory advance + banner advance + push
- Atomic commit: `docs(closeout): v1.6-phase-3 (DFS + Profile Refactor) COMPLETE — O-03 + Y-02 + Y-06 RESOLVED`

**Phase 3 Success Criteria:**
- [ ] `scripts/util/dfs_response.py` NEW + 10+ test (endpoint-aware dispatcher + safe helpers)
- [ ] tech_audit + competitive_analysis migrated (O-03 RESOLVED)
- [ ] `scripts/util/profile_aware_defaults.py` NEW + 12+ test
- [ ] 5+ skill migrate to profile_aware_defaults
- [ ] pytest 1100+ → 1130+ PASS + 10 SKIP (regression sıfır + ~22 yeni test)
- [ ] `.mcp.json` 482B byte-byte korundu (F-16 40+ commit cumulative)
- [ ] DECISIONS.md ≤6144B (no new ADR)
- [ ] plugin.json 1.5.0 unchanged
- [ ] Atomic phase paterni 60 → 61'inci kanıt cumulative

---

## 🚦 Quality Gates (Non-Negotiable)

### Per-Task TDD Cycle (v1.5 brief paterni reuse)
1. **Test yaz (RED)** — test FAIL etmeli (henüz implementation yok)
2. **Minimal implementation (GREEN)** — test PASS etmeli
3. **Refactor (IMPROVE)** — pattern temizliği
4. **pytest 1066+ PASS verify** — regression sıfır

### Per-Phase Commit Pattern
- 1 atomic commit per Tier (Per-Phase plan'da listelendi)
- Closeout commit zorunlu (memory advance + banner update)
- Commit message format: `<type>(<scope>): <body> (<finding-id> <phase-id> Tier <n>)`
- Type: `refactor` / `feat` / `test` / `fix` / `docs` / `chore` / `ci`
- Co-Authored-By footer zorunlu

### Cross-Check Protocol (Lesson 38 v2 + Lesson 67 Stacked, 43+ ardışık kanıt)

Her Tier'da brief execute etmeden ÖNCE:
1. **Brief premise verify** — "iddia X" runtime grep ile doğrula. Brief premise revize OLABİLİR (memory: "brief premise revize 13'üncü v1.5 cycle"; v1.6'da bu sayım devam eder).
2. **File existence** — claim edilen dosyalar gerçekten var mı? `find` + `grep -l` ile cross-check.
3. **Test infrastructure** — yeni test'in kullanacağı fixture/conftest var mı?

Her Tier'da implementation SIRASINDA:
4. **Manager pre-dispatch full-file inspect** — agent dispatch öncesi dosyanın bütünü read'le, satır numaraları ile çalış.
5. **Self-correction** — claim → runtime verify → claim doğru mu yanlış mı? Yanlışsa brief premise revize.

Her Tier KAPANMADAN ÖNCE:
6. **Runtime grep verify** — fix sonrası grep ile pattern eliminated mi (örn. Phase 1 sonrası 4 hook script all exists, EXPECTED_DEFERRED {} empty).
7. **pytest 1066+ PASS** — regression sıfır.
8. **DURUR conditions** verify — listede sayılan tüm invariantlar intact.

### DURUR (no fall-back) Conditions

**Phase boyunca STOP koşulları:**
- pytest baseline regression (1066 → 1065 ya da daha az PASS) → STOP, root cause analyze, fix
- `.mcp.json` byte change → STOP (F-16 invariant breach)
- DECISIONS.md cap aşımı (>6144B) → STOP, ADR consolidation
- plugin.json version drift (1.5.0 unchanged in this milestone) → STOP, version drift breach
- Slug literal grep hit in plugin runtime code → STOP (plugin agnostik breach)
- Schema validation FAIL (`Draft7Validator.check_schema`) → STOP, schema authority bozulamaz
- 16 skill body update sırasında SKILL.md frontmatter ihlali → STOP, F-28 frontmatter validation regression

**Cross-check FAIL koşulları:**
- Brief premise yanlış → revize, NOT bypass
- Runtime grep "yapıldı" iddiasını çürütüyor → STOP, fix
- Memory'deki claim runtime'da false → memory advance, claim correct

### Memory Advance Protocol

Her phase-end'de:
1. `memory/project_current_status.md` — phase COMPLETE entry, engine HEAD hash
2. `memory/project_phase_lessons.md` — yeni Lesson 38 v2 catch (cumulative count update; v1.6'da expected catches 43 → 45+)
3. `memory/MEMORY.md` (index) — phase listesi güncel
4. PHASE_STATUS.md banner advance

---

## 🔄 Cross-Reference Map

### Findings ↔ Files

**Phase 1:**
- Y-03 → `scripts/hooks/check_naming.py` (NEW) + `tests/hooks/test_check_naming.py` (NEW)
- Y-04 → `scripts/hooks/validate_before_write.py` (NEW) + `tests/hooks/test_validate_before_write.py` (NEW)
- Q-V1.5-HOOK-SCRIPTS-MISSING-01 → `tests/hooks/test_hook_scripts_exist.py` EXPECTED_DEFERRED set update + `docs/OPEN_QUESTIONS.md` Q final RESOLVED

**Phase 2:**
- H-E → `schemas/events.schema.json` event_type enum + schema_version bump
- migration 0004 → `scripts/migrations/0004_events_1_0_to_1_1.py` (NEW) + `tests/scripts/test_migration_0004.py` (NEW)
- 16 skill DSL deprecate → `skills/<skill>/SKILL.md` body update (Tier 1 runtime grep ile cumulative liste)

**Phase 3:**
- Y-02 → `scripts/util/dfs_response.py` (NEW) + `tests/util/test_dfs_response.py` (NEW)
- O-03 → `scripts/discovery/tech_audit_transform.py:161` + `scripts/discovery/competitive_analysis_transform.py:519` migrate
- Y-06 → `scripts/util/profile_aware_defaults.py` (NEW) + `tests/util/test_profile_aware_defaults.py` (NEW) + 5+ skill migrate

### ADR References
- ADR-009 — master-excel schema-driven (Phase 3 Y-06 profile cascade — config inheritance discipline)
- ADR-012 — HTTP $id authority (Phase 1 Y-03 schema_id format check)
- ADR-018 — Schema additive bump (Phase 2 H-E events.schema 1.0 → 1.1; paterni reuse)
- ADR-021 — events.jsonl path canonical (Phase 2 migration 0004 atomic write)
- ADR-026 — DECISIONS.md 6144B cap (no new ADR — paterni reuse)
- ADR-035 — PSEO_WORKSPACE_ROOT canonical (Phase 3 Y-06 profile load workspace path)
- ADR-036 — 5-file version sync invariant (Phase 1+2+3 commit'lerde plugin.json 1.5.0 intact)
- ADR-038 — R-XX numbering gap-tolerant (gerekirse F-NN extension)

### Predecessor briefs (paterni reuse)
- `docs/superpowers/plans/v1.5-audit-closure-brief.md` — Tier breakdown + DURUR + Cross-Check protocol verbatim authority
- `docs/superpowers/plans/v1.4-deep-audit-fix-brief.md` — multi-Tier brief paterni + Q-XXX bundle closure paterni
- `docs/superpowers/plans/v1.4-bootstrap-paths-fix-brief.md` — Tier breakdown paterni

---

## ✅ Master Success Criteria (v1.6-Phase 1+2+3 cumulative)

- [ ] **3 Q-XXX RESOLVED** (Q-V1.5-HOOK-SCRIPTS-MISSING-01 final 4-of-4)
- [ ] **3 finding RESOLVED** (Y-03 + Y-04 + H-E + O-03)
- [ ] **3 yeni script DELIVERED** (scripts/util/dfs_response.py + scripts/util/profile_aware_defaults.py + scripts/migrations/0004 — Y-02 + Y-06 + migration)
- [ ] **2 yeni hook DELIVERED** (scripts/hooks/check_naming.py + scripts/hooks/validate_before_write.py — Y-03 + Y-04)
- [ ] **1 schema bump** (events.schema.json 1.0 → 1.1 additive — H-E)
- [ ] **16 skill body update** (canonical event_type emission — H-E DSL deprecate)
- [ ] **5+ skill migrate** to profile_aware_defaults (Y-06)
- [ ] **2 transform migrate** to dfs_response (O-03)
- [ ] **pytest 1066 → 1130+ PASS + 10 SKIP** (regression sıfır + ~64 yeni test cumulative)
- [ ] **`.mcp.json` 482B byte-byte korundu** (F-16 37 → 40+ commit cumulative)
- [ ] **Plugin agnostik 0 slug literal** in plugin runtime code (F-16 grep zero hits)
- [ ] **DECISIONS.md ≤6144B** (yeni ADR yazılmadıysa 6126B unchanged ÖNERİLİR)
- [ ] **plugin.json 1.5.0 unchanged** (v1.6 milestone bu brief'in scope'u DEĞİL — final v1.6 release commit ayrı session'da)
- [ ] **3 closeout commit** + memory advance + PHASE_STATUS banner per phase
- [ ] **Atomic phase paterni 58 → 61+ kanıt** (3 yeni phase consecutive convergent)
- [ ] **Lesson 38 v2 cumulative count update** (43 → 45+ expected catches)

---

## 🚀 Kickoff Strategy

**Önerilen sıra:** Phase 1 → Phase 2 → Phase 3 (sequential, ayrı session'lar).

**Sıralama gerekçesi:**
1. **Phase 1 önce:** Hook'lar yazıldıktan sonra Phase 2 + Phase 3 commit'leri bu hook'ların gate'inden geçer (16 skill body update + 5+ skill migrate sırasında naming + schema-first violations otomatik yakalanır). Bu order Lesson 38 v2 trajectory'yi optimize eder.
2. **Phase 2 sonra:** events.schema bump 16 skill'i etkiler; Phase 3'teki Y-06 skill migrate'i bu skill body update'inden sonra gelir (sıraya gerek YOK ama mantıksal akış: schema → skill body → util migrate).
3. **Phase 3 son:** Refactor + tech debt cleanup; en az kullanıcı görünür değer ama en yüksek kod sağlığı. Phase 1+2 gate guard'ları ile güvenli.

**Her session başlangıcı:**
1. Wakeup sequence oku (15 madde)
2. THIS BRIEF Section "Phase X" oku
3. Tier 1'den başla, sıralı git
4. Her Tier'da TDD red-green + cross-check + atomic commit
5. Phase-end closeout commit
6. Push to origin/main (engine repo PRIVATE)

**Her session sonu:**
1. Memory advance
2. PHASE_STATUS banner advance
3. Atomic phase paterni cumulative count update

**Final v1.6 milestone closeout (Phase 3 sonrası, opsiyonel):**
- v1.6 final release commit (plugin.json + marketplace.json 1.5.0 → 1.6.0 + RELEASE_NOTES_v1.6.0.md NEW + README/INSTALL banner update via Y-05 first apply v1.6 cycle)
- Bu brief'in scope'u DEĞİL — Phase 1+2+3 tamamlanınca **ayrı v1.6-marketplace-publication-wave-1** session'ı.

---

## 📚 Cross-References

- **Predecessor:** v1.5-audit-closure-brief.md (Phase 1+2+3 successful execution paterni reuse — own-paterni reuse)
- **v1.5.0 closeout state:** docs/RELEASE_NOTES_v1.5.0.md, docs/PHASE_STATUS.md (v1.5.0 MILESTONE CLOSED banner)
- **Engine HEAD baseline:** `2479a94` (post-v1.5.0 closeout commit, PUSHED + git tag `v1.5.0`)
- **Test infrastructure references:**
  - `tests/scripts/conftest.py` — fixture conventions
  - `tests/state/test_events_schema_compliance.py` — schema instance test paterni
  - `tests/scripts/test_migration_0001/0002/0003.py` — migration test paterni reuse for 0004
  - `tests/hooks/test_hook_scripts_exist.py` — EXPECTED_DEFERRED paterni
- **Spec references:**
  - `rules/skills.md` — Skill body discipline (multi-block format)
  - `rules/append-only-state.md` — events.jsonl + workflows JSON discipline
  - `rules/schema-first.md` — Y-04 hook authority (Schema PR > implementation PR sırası)
  - `rules/single-source-of-truth.md` — Y-06 profile cascade SSOT authority
  - `rules/naming.md` — Y-03 hook authority (slug regex + per-file-type pattern)
  - `rules/excel-discipline.md` — master.xlsx writer policy

---

**Brief authoring complete. Fresh session kickoff prompts: see "Fresh Session Resume Prompts" section below for chat-paste templates per phase. Engine HEAD baseline: `2479a94` (v1.5.0 MILESTONE CLOSED, PUSHED + tag v1.5.0).**

---

# Fresh Session Resume Prompts (chat-paste hazır)

Üç ayrı fresh session başlatmak için aşağıdaki prompt'lar copy-paste hazırdır. Her phase için ayrı session aç, prompt'u yapıştır, manager session başlasın.

## 📋 Phase-1 Fresh Session Prompt (Hook Authoring)

```
Sen v1.6-Phase-1 (Phase 13 Hook Authoring) milestone'unu yöneten manager session'sın.
Kalite non-negotiable: TDD red-green her task için + Lesson 38 v2 cross-check + atomic
commit per phase + memory advance per milestone-end + runtime grep verify her wave-end.

ZORUNLU OKUMA SIRASI (skip YASAK):
1. memory/MEMORY.md + memory/project_current_status.md
   (v1.5.0 MILESTONE CLOSED 2026-05-07 → engine HEAD `2479a94` PUSHED + tag v1.5.0;
   12 atomic commit cumulative v1.5 cycle; pytest 1066 PASS + 10 SKIP baseline;
   Lesson 38 v2 30→43 cumulative catches)
2. docs/RELEASE_NOTES_v1.5.0.md (v1.5 cumulative kapsam + drift baseline)
3. docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
   → Section "v1.6-Phase-1: Phase 13 Hook Authoring" (Tier 1-4 verbatim)
   → Section "Quality Gates" + "Cross-Check Protocol" + "DURUR conditions"
4. docs/PHASE_STATUS.md (son banner: v1.5.0 MILESTONE CLOSED)
5. docs/OPEN_QUESTIONS.md (Q-V1.5-HOOK-SCRIPTS-MISSING-01 partial-2-of-4 RESOLVED state)
6. rules/naming.md (Y-03 check_naming.py rule body authority)
7. rules/schema-first.md (Y-04 validate_before_write.py rule body authority)
8. tests/hooks/test_hook_scripts_exist.py (EXPECTED_DEFERRED set update target)
9. scripts/hooks/check_append_only.sh + scripts/hooks/check_excel_writer.py
   (paterni reuse — already-authored 2-of-4 hook reference)

MISSION: docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
"v1.6-Phase-1: Phase 13 Hook Authoring" execute (~90-120 dk).
Tier breakdown verbatim:
- Tier 1: scripts/hooks/check_naming.py NEW + tests/hooks/test_check_naming.py NEW
  (Y-03; ~30-45 dk; 8+ test slug regex per file type)
- Tier 2: scripts/hooks/validate_before_write.py NEW + tests/hooks/test_validate_before_write.py
  NEW (Y-04; ~30-45 dk; 6+ test schema-first enforcement)
- Tier 3: Hook config registration + EXPECTED_DEFERRED set {} empty + Q-V1.5-HOOK-SCRIPTS-
  MISSING-01 RESOLVED final (4-of-4) (~10-15 dk)
- Tier 4: Closeout (~15 dk) — memory + banner + push

KRİTİK KURALLAR (DURUR conditions):
- pytest 1066 PASS + 10 SKIP baseline regression SIFIR
- .mcp.json 482B byte-byte korunma F-16 invariant (37+ commit cumulative)
- plugin.json 1.5.0 unchanged (v1.6 final commit AYRI session'da)
- DECISIONS.md ≤6144B (mevcut 6126B, 18B headroom; yeni ADR yazma ÖNERİLMEZ)
- 0 slug literal in plugin runtime code (F-16 invariant)
- Hook script Python 3.x compatible + macOS bash 3.2 portable (lesson 38 v2 #30 paterni)

DURUR (no fall-back):
- pytest regression / .mcp.json byte change / DECISIONS cap aşımı / plugin.json
  version drift / slug literal hit / schema validation FAIL → STOP, root cause, fix.

Cross-Check Protocol (Lesson 38 v2 43+ ardışık kanıt):
- Her Tier'da brief execute etmeden ÖNCE: brief premise runtime grep ile doğrula
  (memory: brief premise revize 13'üncü v1.5 cycle cumulative; v1.6'da devam).
- Manager pre-dispatch full-file inspect — agent dispatch öncesi dosyanın bütünü read'le.
- Hook scripts macOS bash 3.2 portable test (mapfile YASAK; while-read paterni paterni reuse).
- Her Tier kapanmadan ÖNCE: runtime grep verify + pytest 1080+ PASS regression sıfır.

Phase 1 Success Criteria:
- scripts/hooks/check_naming.py NEW + 8+ test
- scripts/hooks/validate_before_write.py NEW + 6+ test
- 4-of-4 hooks active (.claude/settings.json hook config)
- tests/hooks/test_hook_scripts_exist.py EXPECTED_DEFERRED {} empty
- docs/OPEN_QUESTIONS.md Q-V1.5-HOOK-SCRIPTS-MISSING-01 RESOLVED final
- pytest 1066 → 1080+ PASS + 10 SKIP (regression sıfır + ~14 yeni test)
- atomic phase paterni 59'uncu kanıt cumulative

Phase 1 tamamlanınca closeout commit + memory advance + PHASE_STATUS banner advance
+ push to origin/main.

Brief'i komple oku, sonra Tier 1'den başla. Acele YASAK — titiz cross-checkli ilerle.
"en iyi senaryo + senin seçimlerinle devam et titiz cross check yapmayı unutma"
(Süleyman feedback enforce default — v1.5 cycle 13 catch cumulative kanıt).
```

## 📋 Phase-2 Fresh Session Prompt (events.schema enum bump)

```
Sen v1.6-Phase-2 (events.schema enum bump 1.0→1.1) milestone'unu yöneten manager session'sın.
Kalite non-negotiable: TDD red-green her task için + Lesson 38 v2 cross-check + atomic
commit per phase + memory advance per milestone-end + runtime grep verify her wave-end.

ZORUNLU OKUMA SIRASI (skip YASAK):
1. memory/MEMORY.md + memory/project_current_status.md
   (v1.6-Phase-1 COMPLETE state, engine HEAD <Phase-1 closeout sha>)
2. docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
   → Section "v1.6-Phase-2: events.schema enum bump" (Tier 1-5 verbatim)
   → Section "Quality Gates" + "Cross-Check Protocol" + "DURUR conditions"
3. docs/PHASE_STATUS.md (Phase-1 banner)
4. schemas/events.schema.json (Phase-2 baseline event_type enum)
5. scripts/state/migrate_legacy_events.py + scripts/migrations/0001..0003
   (paterni reuse: importlib.util.spec_from_file_location numeric-prefix paterni)
6. tests/scripts/test_migration_0001/0002/0003.py (paterni reuse)
7. tests/state/test_events_schema_compliance.py (compliance test paterni)
8. rules/append-only-state.md (events.jsonl atomic write discipline)
9. docs/DECISIONS.md ADR-018 (Schema additive bump paterni)

MISSION: docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
"v1.6-Phase-2: events.schema enum bump" execute (~120-180 dk).
Tier breakdown verbatim:
- Tier 1: schemas/events.schema.json schema_version 1.0→1.1 + event_type enum 16 yeni
  canonical entry (additive) + tests/schemas/test_events_schema_event_type_enum_v1_1.py
  NEW 5+ test (~30-45 dk)
- Tier 2: scripts/migrations/0004_events_1_0_to_1_1.py NEW + tests/scripts/test_migration_
  0004.py NEW 9+ test (note=[skill=X] DSL migration; paterni 0001/0002/0003 reuse) (~45-60 dk)
- Tier 3: 16 skill body update canonical event_type emission (workaround DSL deprecate)
  (~30-45 dk)
- Tier 4: tests/state/test_events_schema_compliance.py update v1.1 + ADR-018 cite (~10-15 dk)
- Tier 5: Closeout (~15 dk)

KRİTİK KURALLAR (DURUR conditions):
- pytest 1080+ PASS baseline (post-Phase-1) regression SIFIR
- .mcp.json 482B byte-byte korunma F-16 invariant (38+ commit cumulative)
- plugin.json 1.5.0 unchanged
- DECISIONS.md ≤6144B (ADR-018 cite update only, no new ADR)
- 0 slug literal in plugin runtime code
- 16 skill body update sırasında F-28 frontmatter validation regression YASAK
- events.schema schema_version const "1.1" exactly (additive bump, not major rewrite)

DURUR (no fall-back):
- pytest regression / .mcp.json byte change / DECISIONS cap aşımı / plugin.json
  version drift / slug literal hit / schema validation FAIL / SKILL.md frontmatter
  invalid → STOP, root cause, fix.

Cross-Check Protocol (Lesson 38 v2 43+ ardışık kanıt):
- Tier 1 öncesi: 16 skill list runtime grep — `grep -rn "event_type.*manual.*note.*skill"
  skills/ scripts/`. Brief premise revize candidate (16 sayısı runtime'da değişebilir).
- Tier 2 öncesi: events.schema instance count `find ~/Documents/platinum-seo-workspace -name
  "events.jsonl"` — migration test data scope.
- Tier 3 öncesi: skill body multi-block format inspect (lesson 8 v6+ paterni; partial inspect
  YASAK).
- Her Tier kapanmadan: runtime grep verify + pytest 1100+ PASS.

Phase 2 Success Criteria:
- schemas/events.schema.json 1.0 → 1.1 + 16 yeni canonical event_type
- scripts/migrations/0004 NEW + 9+ test
- 16 skill body update (workaround DSL deprecate)
- tests/state/test_events_schema_compliance.py v1.1 update
- pytest 1080+ → 1100+ PASS + 10 SKIP (regression sıfır + ~10 yeni migration test)
- atomic phase paterni 60'ıncı kanıt cumulative

Phase 2 tamamlanınca closeout commit + memory advance + PHASE_STATUS banner advance
+ push to origin/main.

Brief'i komple oku, sonra Tier 1'den başla. Acele YASAK — titiz cross-checkli ilerle.
"en iyi senaryo + senin seçimlerinle devam et titiz cross check yapmayı unutma"
(Süleyman feedback enforce default).
```

## 📋 Phase-3 Fresh Session Prompt (DFS + Profile Refactor)

```
Sen v1.6-Phase-3 (DataForSEO + Profile Refactor) milestone'unu yöneten manager session'sın.
Kalite non-negotiable: TDD red-green her task için + Lesson 38 v2 cross-check + atomic
commit per phase + memory advance per milestone-end + runtime grep verify her wave-end.

ZORUNLU OKUMA SIRASI (skip YASAK):
1. memory/MEMORY.md + memory/project_current_status.md
   (v1.6-Phase-2 COMPLETE state, engine HEAD <Phase-2 closeout sha>)
2. docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
   → Section "v1.6-Phase-3: DataForSEO + Profile Refactor" (Tier 1-4 verbatim)
   → Section "Quality Gates" + "Cross-Check Protocol" + "DURUR conditions"
3. docs/PHASE_STATUS.md (Phase-2 banner)
4. scripts/discovery/tech_audit_transform.py:161 (_normalize_dfs_response — Lighthouse +
   page_metrics + audits handle; Lesson 38 v2 #35 semantic-divergent extension cite)
5. scripts/discovery/competitive_analysis_transform.py:519 (_local_normalize_dfs —
   import-isolation fallback; Lesson 38 v2 #34 by-design fallback cite)
6. scripts/util/url_normalize.py (paterni reuse: 9 caller × 1 canonical adapter pattern;
   error class divergence handling)
7. scripts/discovery/quickwins_transform.py + diğer 4-5 transform inline default config'leri
   (Y-06 extract candidate; runtime grep cumulative liste)
8. memory/project_phase_lessons.md (Lesson 38 v2 cumulative paterni; brief premise revize
   13'üncü v1.5 cycle, v1.6'da devam)

MISSION: docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md
"v1.6-Phase-3: DataForSEO + Profile Refactor" execute (~180-240 dk).
Tier breakdown verbatim:
- Tier 1 Y-02: scripts/util/dfs_response.py NEW + tests/util/test_dfs_response.py NEW
  10+ test (endpoint-aware dispatcher: keyword vs Lighthouse vs unknown; safe_int/float/str
  helpers) (~45-60 dk)
- Tier 2 O-03: tech_audit + competitive_analysis migrate to scripts.util.dfs_response
  (CAUTION: competitive_analysis fallback by-design preserve; semantic divergence respect)
  (~30-45 dk)
- Tier 3 Y-06: scripts/util/profile_aware_defaults.py NEW + 5+ skill migrate (~60-90 dk)
- Tier 4: Closeout (~15 dk)

KRİTİK KURALLAR (DURUR conditions):
- pytest 1100+ PASS baseline (post-Phase-2) regression SIFIR
- .mcp.json 482B byte-byte korunma F-16 invariant (39+ commit cumulative)
- plugin.json 1.5.0 unchanged
- DECISIONS.md ≤6144B (no new ADR)
- 0 slug literal in plugin runtime code
- O-03 migration sırasında competitive_analysis fallback paterni preserve (semantic divergence)
- Y-06 5+ skill migrate sırasında F-28 SKILL.md frontmatter invariant intact

DURUR (no fall-back):
- pytest regression / .mcp.json byte change / DECISIONS cap aşımı / plugin.json
  version drift / slug literal hit / schema validation FAIL / fallback semantik kaybı →
  STOP, root cause, fix.

Cross-Check Protocol (Lesson 38 v2 43+ ardışık kanıt):
- Tier 1 öncesi: tech_audit + competitive_analysis full file inspect — semantic divergence
  paterni doğrula (Lesson 38 v2 #34-35 v1.5-Phase-1 cite).
- Tier 1 sonrası: dfs_response endpoint dispatcher response shape detection sıkı tested
  (false dispatch — semantic kayıp riski yüksek).
- Tier 2 sırasında: competitive_analysis fallback `_local_normalize_dfs` korunmalı —
  silmek YASAK (semantic divergence paterni; import-isolation by-design).
- Tier 3 öncesi: 5+ skill list runtime grep — `grep -rn "profile.*default\|inline default"
  scripts/discovery/ scripts/planning/`. Brief premise revize candidate (5+ sayısı
  runtime'da değişebilir).
- Tier 3 design discussion: API kontratı (load_profile + cascade_defaults + override)
  TBD by manager session — Süleyman onay turu OPSİYONEL (5+ skill paterni runtime'dan
  netlik gelirse otomatik karar).

Phase 3 Success Criteria:
- scripts/util/dfs_response.py NEW + 10+ test (endpoint-aware dispatcher)
- tech_audit + competitive_analysis migrated (O-03 RESOLVED)
- scripts/util/profile_aware_defaults.py NEW + 12+ test
- 5+ skill migrate to profile_aware_defaults
- pytest 1100+ → 1130+ PASS + 10 SKIP (regression sıfır + ~22 yeni test)
- atomic phase paterni 61'inci kanıt cumulative

Phase 3 tamamlanınca closeout commit + memory advance + PHASE_STATUS banner advance
+ push to origin/main.

v1.6 final marketplace publication closeout (plugin.json 1.5.0 → 1.6.0 +
RELEASE_NOTES_v1.6.0.md NEW + 5-file sync via Y-05 first apply v1.6 cycle) AYRI
v1.6-marketplace-publication-wave-1 session'ında — Phase 3 scope DEĞİL.

Brief'i komple oku, sonra Tier 1'den başla. Acele YASAK — titiz cross-checkli ilerle.
"en iyi senaryo + senin seçimlerinle devam et titiz cross check yapmayı unutma"
(Süleyman feedback enforce default).
```

---

**v1.6 Hardening + Hooks + Refactor Brief authoring complete.** Brief docs/superpowers/plans/v1.6-hardening-hooks-refactor-brief.md NEW. Atomic commit + push origin main + 3 fresh session resume prompts ready for chat-paste.
