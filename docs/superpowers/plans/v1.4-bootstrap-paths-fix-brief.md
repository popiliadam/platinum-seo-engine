# Manager Session Brief — Bootstrap Path Modernization (v1.4 milestone)

**Yazım tarihi:** 2026-05-07 (post-v1.3-Hooks-Overhaul closeout, engine HEAD `7665d4a` PUSHED)
**Manager session role:** Q-V1.4-BOOTSTRAP-PATHS-01 fix Seçenek A — `bootstrap_project.py` `build_project_config()` 7 path default'ını modern workspace convention'a güncelle. Engine repo değişikliği + yeni unit test dosyası + init-project skill audit + closeout.
**Estimated duration:** ~120-150 dk (Tier 1: 30-45 dk + Tier 2: 45-60 dk + Tier 3 opsiyonel: 30 dk + closeout: 15 dk)

---

## 🎯 Hedef

`scripts/state/bootstrap_project.py` `build_project_config()` (line 44) içinde **7 path field default'ı LEGACY hardcoded** — Phase 6+ skill'lerin (sf-import, gsc-pull, dfs-pull, quick-wins, content-decay) okuduğu **modern workspace convention** ile uyuşmuyor. Eykom onboarding'de keşfedildi (2'inci proje portfolio'ya eklenirken `/pseo-init` invocation), workaround manuel post-process patch ile schema-valid modern config üretildi. Kalıcı kök fix bu brief'in scope'u.

**Senaryo:**
- 7 path default modernize: legacy → modern convention (engine repo değişikliği)
- `tests/state/test_bootstrap_project.py` **NEW** dosya oluştur (mevcut DEĞİL — Q metnindeki "mevcut assertion'lar" iddiası invalidate, runtime grep ile doğrulandı)
- `init-project` SKILL.md §Step 4 cross-check (subprocess CLI invoke; post-fix verify)
- Eykom workspace runtime smoke test (post-fix bootstrap ürettiği config dentnotion modern conv ile aynı mı)

**KRİTİK:**
- pytest baseline 743+9 (post-v1.3-Hooks-Overhaul) — fix sonrası **regression yok zorunlu**
- `.mcp.json` 482B byte-byte korunma F-16 invariant zorunlu (28+ commit cumulative)
- ADR-036 5-file sync 1.3.0 invariant intact (script-only edit version_field etkilemez)
- DECISIONS.md 6112B unchanged önerilir (32B headroom; yeni ADR yazılmaması — mevcut ADR-009 master-excel schema-driven + ADR-021 events.jsonl path canonical paterni reuse)
- Plugin agnostik discipline: F-16 invariant — engine repo path workspace içinde olmamalı

---

## 📋 Wakeup Sequence (zorunlu okuma sırası)

1. `memory/MEMORY.md` + `memory/project_current_status.md` — v1.3-Hooks-Overhaul COMPLETE state, engine HEAD `7665d4a` PUSHED
2. `docs/PHASE_STATUS.md` — son banner: "v1.3-Hooks-Overhaul COMPLETE — v1.4+ feature backlog NEXT"
3. `docs/OPEN_QUESTIONS.md` — Q-V1.4-BOOTSTRAP-PATHS-01 [P1] (line 5-15) tam içerik + 4 seçenek
4. **`scripts/state/bootstrap_project.py`** — 187 satır; `build_project_config(args)` line 44; paths defaults line 65-71 (7 field hardcoded)
5. **`schemas/project-config.schema.json`** — paths properties + required: 7 field aynen (workspace_root, excel_filename, sf_exports_dir, staging_dir, reports_dir, blog_dir, backups_dir)
6. **`skills/meta/init-project/SKILL.md`** — Step 4 (line 134) subprocess CLI invoke `python3 scripts/state/bootstrap_project.py`; DURUR conditions line 288
7. **dentnotion workspace gerçek path inceleme** — modern convention reference (PSEO_WORKSPACE_ROOT/projects/dentnotion/...)
   - `master.xlsx` (lowercase F1 workbook policy)
   - `inbox/sf` (Screaming Frog exports)
   - `_state/cache` (staging area)
   - `outputs/reports`
   - `outputs/content/drafts` (was blog/)
   - `_state/backups`
8. **eykom workspace post-workaround state** — workflow `eykom-2026-05-07-8c5b`, master.xlsx SHA-256 `f18fc6f6ffde3387b9349441bbff3d6301a0d6b9a6d03af7c383ddf40bc9f3f6`; modern convention applied
9. `rules/single-source-of-truth.md` — workspace path convention authority
10. `rules/excel-discipline.md` — `master.xlsx` (lowercase) F1 workbook policy
11. `tests/state/` — mevcut test dosyaları (`__pycache__/` + `test_events_schema_compliance.py` only; `test_bootstrap_project.py` YOK)
12. `docs/DECISIONS.md` — runtime byte 6112B (32B headroom; ADR-009 master-excel schema-driven + ADR-021 events.jsonl path canonical reuse)
13. `docs/DECISIONS_ARCHIVE.md` — ADR-009 + ADR-021 + ADR-035 (PSEO_WORKSPACE_ROOT canonical) cross-ref
14. `.claude-plugin/plugin.json` — version 1.3.0 (ADR-036 invariant; script-only fix etkilemez)
15. **eykom config baseline** — `projects/eykom/project.config.json` (post-workaround modern paths reference)

---

## 🔴 Bulgu Detayı — Q-V1.4-BOOTSTRAP-PATHS-01 P1

### Mevcut Legacy Default'lar (`bootstrap_project.py:65-71`)

```python
"paths": {
    "workspace_root": workspace_root,  # env override OR ~/Documents/platinum-seo-engine/projects/{slug}
    "excel_filename": f"{slug}_MASTER.xlsx",   # BÜYÜK harf legacy
    "sf_exports_dir": "sf-exports",            # legacy flat
    "staging_dir": "staging",                  # legacy flat
    "reports_dir": "reports",                  # legacy flat
    "blog_dir": "blog",                        # eski naming
    "backups_dir": "_backups",                 # legacy flat
},
```

### Hedef Modern Convention (dentnotion workspace runtime kanıt)

```python
"paths": {
    "workspace_root": workspace_root,         # PSEO_WORKSPACE_ROOT env (zaten var) — fallback {workspace}/projects/{slug}
    "excel_filename": "master.xlsx",          # küçük harf F1 workbook policy
    "sf_exports_dir": "inbox/sf",             # nested under inbox/
    "staging_dir": "_state/cache",            # nested under _state/
    "reports_dir": "outputs/reports",         # nested under outputs/
    "blog_dir": "outputs/content/drafts",     # outputs/content/drafts (eskiden blog/)
    "backups_dir": "_state/backups",          # nested under _state/
},
```

### Niye Bu Drift Var?

- bootstrap_project.py Phase 1'de yazıldı (early scaffold, flat structure)
- Phase 6+ ingestion skills (sf-import, gsc-pull, dfs-pull) modern nested convention adopted
- Schema (`project-config.schema.json` v1.3) sadece field'ların **var olmasını** validate ediyor, **değerlerini** değil → drift validate-time'da yakalanmıyor
- Pilot proje dentnotion manuel modern convention'a geçirilmiş (ne zaman ne commit'te kayıt yok)
- Multi-project test (eykom 2'inci proje) drift'i ortaya çıkardı

### Eykom Workaround (Mevcut State)

`bootstrap_project.py --dry-run` + post-process patch (paths field manuel override) → schema-valid modern config:
- Workflow ID: `eykom-2026-05-07-8c5b`
- Master.xlsx SHA-256: `f18fc6f6ffde3387b9349441bbff3d6301a0d6b9a6d03af7c383ddf40bc9f3f6`
- Eykom çalışıyor ama her yeni proje onboarding'de aynı manuel adım gerekecek → tekrarlayan drift kaynağı

---

## 🌊 Tier Breakdown (2 Tier + closeout, opsiyonel Tier 3)

### Tier 1 (30-45 dk) — bootstrap_project.py modernize + cross-check

**Amaç:** 7 path default modernize + runtime smoke test (eykom workspace post-fix bootstrap ürettiği config dentnotion modern conv ile bire-bir uyumlu).

**Kapsam:**
1. `scripts/state/bootstrap_project.py` `build_project_config()` 7 path default güncelle:
   - `excel_filename`: `{slug}_MASTER.xlsx` → `master.xlsx`
   - `sf_exports_dir`: `sf-exports` → `inbox/sf`
   - `staging_dir`: `staging` → `_state/cache`
   - `reports_dir`: `reports` → `outputs/reports`
   - `blog_dir`: `blog` → `outputs/content/drafts`
   - `backups_dir`: `_backups` → `_state/backups`
   - `workspace_root`: PSEO_WORKSPACE_ROOT env (DEĞİŞMEZ — zaten var); fallback `f"{workspace_root_env}/projects/{slug}"` paterni opsiyonel — engine repo path fallback **kaldırılmalı** (F-16 invariant)
2. Header docstring update — modern convention referans
3. Runtime smoke test:
   - `python3 scripts/state/bootstrap_project.py --project test_dispatch --domain example.com --market TR --locale tr-TR --currency TRY --platform wordpress --dry-run` → STDOUT'ta paths field modern convention
   - JSON dump diff vs dentnotion canonical → 0 path mismatch
4. Schema validate roundtrip: bootstrap çıktısı `project-config.schema.json` v1.3 ile valid
5. Eykom workaround obsolete kanıt: bootstrap output direkt schema-valid + modern conv (post-process patch artık gerekli değil)

**Atomic commit:** `fix(state): bootstrap_project.py 7 path default modern convention (Q-V1.4-BOOTSTRAP-PATHS-01 Tier 1)`

**DURUR conditions:**
- pytest baseline 743+9 regression → DURUR
- `.mcp.json` byte change → DURUR (F-16 invariant)
- ADR-036 5-file sync 1.3.0 etkileme → DURUR (script-only edit beklenmiyor; doğrulama zorunlu)
- Schema validate roundtrip FAIL → DURUR (paths shape değişti, schema desteklemiyorsa)

### Tier 2 (45-60 dk) — `tests/state/test_bootstrap_project.py` NEW

**Amaç:** Q metnindeki "mevcut path assertion'ları güncellenmeli" iddiası invalidate (test dosyası yok); yeni dosya oluştur, modern convention regression-lock.

**Kapsam:**
1. **`tests/state/test_bootstrap_project.py` NEW** — yeni unit test dosyası:
   - `test_build_project_config_paths_modern_convention` — 7 path default modern conv match
   - `test_excel_filename_lowercase_master_xlsx` — F1 workbook policy lock (`master.xlsx`, BÜYÜK harf yasak)
   - `test_sf_exports_dir_nested_inbox` — `inbox/sf` (flat `sf-exports` yasak)
   - `test_staging_dir_nested_state_cache` — `_state/cache` lock
   - `test_reports_dir_nested_outputs` — `outputs/reports` lock
   - `test_blog_dir_outputs_content_drafts` — `outputs/content/drafts` lock (eski naming yasak)
   - `test_backups_dir_nested_state_backups` — `_state/backups` lock
   - `test_workspace_root_env_override` — `PSEO_WORKSPACE_ROOT` env override honored
   - `test_workspace_root_no_engine_repo_path` — engine repo path fallback YASAK (F-16 invariant)
   - `test_schema_validate_roundtrip` — bootstrap output project-config.schema v1.3 ile valid
2. Cross-check fixture: dentnotion canonical project.config.json sample (workspace path convention authority)
3. Schema-first validation paterni reuse — output validate without runtime CLI invocation

**Atomic commit:** `test(state): test_bootstrap_project.py NEW — modern convention regression lock (Q-V1.4-BOOTSTRAP-PATHS-01 Tier 2)`

**DURUR conditions:**
- Yeni test'ler kendi içinde fail → DURUR (test logic hata)
- Tier 1 fix breakage → DURUR (test'in regression-lock işlevi)

### Tier 3 (30 dk OPSIYONEL) — Multi-project capability e2e smoke

**Amaç:** Eykom workaround obsolete kanıt + 3'üncü proje smoke test (yeni bootstrap modernize ile end-to-end onboarding pipeline).

**Kapsam:**
1. Geçici 3'üncü proje slug seç (örn. `test-mp-3`)
2. `python3 scripts/state/bootstrap_project.py --project test-mp-3 --domain test.example.com --market TR --locale tr-TR --currency TRY --platform wordpress` runtime invoke
3. Çıktı `projects/test-mp-3/project.config.json` → modern convention 7 path verify (no manual patch)
4. `init-project` skill SKILL.md §Step 4 ile uyumlu (subprocess CLI exit 0)
5. Geçici proje cleanup (test scope'u kalıcı pollution etmesin)

**Bonus scope:** Süleyman'ın v1.4 backlog'da multi-project capability test (~120 dk ayrı session) ile **bu brief birleştirilebilir** — bootstrap fix + multi-project e2e aynı session'da bağımlı işler. Karar Süleyman'ın.

**Atomic commit:** `test(integration): multi-project e2e smoke (Q-V1.4-BOOTSTRAP-PATHS-01 Tier 3 opsiyonel)`

**DURUR conditions:**
- Tier 1+2 PASS olmadan Tier 3 başlanmaz
- Geçici proje cleanup atlandı → DURUR (workspace pollution)

### Closeout (15 dk)

**Kapsam:**
1. `docs/OPEN_QUESTIONS.md` — Q-V1.4-BOOTSTRAP-PATHS-01 RESOLVED marker (Seçenek A applied)
2. `docs/PHASE_STATUS.md` banner advance: "v1.4-Bootstrap-Paths-Fix COMPLETE" → Active Phase
3. Memory `project_current_status.md` + `MEMORY.md` pointer update
4. Eykom workspace audit not — workaround obsolete kanıt; gelecek `/pseo-init` rerun bootstrap modernize ile direkt çalışır (manuel post-process patch artık gerekli değil)

**Atomic commit:** `docs(closeout): v1.4-bootstrap-paths-fix COMPLETE — Q-V1.4-BOOTSTRAP-PATHS-01 RESOLVED + banner advance`

---

## 🌊 Atomic Strategy

**3 atomic commit per Tier (v1.3-hooks-overhaul-Tier paterni reuse):**

| Tier | Commit | Kapsam | Süre |
|---|---|---|---|
| 1 | `fix(state)` | `bootstrap_project.py` 7 path modernize | 30-45 dk |
| 2 | `test(state)` | `test_bootstrap_project.py` NEW (~10 test) | 45-60 dk |
| 3 (opsiyonel) | `test(integration)` | Multi-project e2e smoke | 30 dk |
| Closeout | `docs(closeout)` | OQ RESOLVED + banner advance + memory finalize | 15 dk |

**Niye per-Tier commit:** Discrete scope; granular rollback + bisect; Süleyman wave-end onay gate'leri net (v1.3 hook overhaul paterni 4 atomic + closeout).

---

## ✅ Acceptance Criteria

1. ✅ **Tier 1:** bootstrap_project.py 7 path default modern convention runtime smoke test PASS — `--dry-run` çıktı dentnotion canonical ile bire-bir uyum
2. ✅ **Tier 1:** Engine repo path fallback (`~/Documents/platinum-seo-engine/projects/{slug}`) **kaldırıldı** — F-16 invariant intact
3. ✅ **Tier 1:** Schema validate roundtrip — bootstrap çıktı `project-config.schema.json` v1.3 ile valid (paths shape değişmeyen, sadece value değişti)
4. ✅ **Tier 2:** `tests/state/test_bootstrap_project.py` NEW — collection 1/1 + ~10 test PASS
5. ✅ **Tier 2:** Test'ler 7 path field için ayrı ayrı assertion (modern convention regression-lock)
6. ✅ **Tier 2:** PSEO_WORKSPACE_ROOT env override ve engine repo path fallback YASAK assertion
7. ✅ **Tier 3 opsiyonel:** 3'üncü proje (`test-mp-3`) bootstrap exit 0 + modern config + cleanup
8. ✅ pytest baseline regression yok (743+9 unchanged or positive drift +10 yeni Tier 2 test)
9. ✅ `.mcp.json` 482B byte-byte korundu (F-16 invariant)
10. ✅ DECISIONS.md byte unchanged (no new ADR — ADR-009 + ADR-021 paterni reuse)
11. ✅ ADR-036 5-file sync 1.3.0 invariant intact (script-only edit)
12. ✅ Plugin agnostik: 0 slug literal grep (bootstrap_project.py modernize sonrası)
13. ✅ Q-V1.4-BOOTSTRAP-PATHS-01 RESOLVED — Seçenek A applied + closeout marker
14. ✅ Atomic phase paterni 43+44+(45 opsiyonel)+46 yeni kanıt cumulative; 47-49 phase consecutive convergent
15. ✅ Eykom workaround obsolete kanıt — manuel post-process patch artık gerekli değil

---

## 🚫 Constraints

- **F-16 invariant:** `.mcp.json` 482B byte-byte korundu (Tier 1+2+3 boyunca)
- **Engine repo path fallback YASAK:** `~/Documents/platinum-seo-engine/projects/{slug}` legacy fallback kaldırılmalı; PSEO_WORKSPACE_ROOT env yoksa **error** veya `.` cwd fallback (script-execution-time karar)
- **No new ADR (DECISIONS.md headroom 32B):** ADR-009 (master-excel.xlsx schema-driven) + ADR-021 (events.jsonl _state/ path canonical) + ADR-035 (PSEO_WORKSPACE_ROOT canonical 1y shim) paterni reuse
- **No skill body modification:** Tier 1+2 scope'u sadece script + test; init-project SKILL.md Step 4 audit ama edit gerekmemeli (subprocess CLI invoke kontratı aynı)
- **Plugin agnostik discipline:** F-16 invariant 28+ commit cumulative — slug literal yasak (test dosyası ve script defaults için generic)
- **Append-only state intact:** events.jsonl audit_action mapping yeni semantic etkilemiyor (Tier 1 hooks scope dışı)
- **Lesson 38 v2 enforcement:** brief premise'i frozen kabul etme; runtime grep her edit'te (Q metni "mevcut test assertion" iddiası invalidate edildi — Tier 2'de NEW olarak yazılıyor; gelecek catch'lere açık ol)
- **Lesson 67 enforcement:** agent worker output ≠ kanıt; karar verici doğrudan verify before commit
- **Schema-first override paterni:** schema'da `paths.*` `pattern`/`enum` constraint EKLENMEZ (Q metni Seçenek C reddedildi, esneklik korunuyor); regression-lock test seviyesinde
- **Eykom workspace dokunulmaz:** workaround config çalışıyor — post-fix `/pseo-init` rerun ile bootstrap modernize uyumlu sonuç üretmeli, ama eykom'un mevcut config'ine dokunulmaz (idempotent: rerun aynı sonuç)
- **dentnotion canonical reference:** modern convention authority — değişiklik yapılmaz (path lock)

---

## 🤔 Süleyman'a Sorulacaklar

### Pre-dispatch (4 soru)

1. **Tier scope:** 2 Tier (~75-105 dk) yoksa 3 Tier dahil multi-project e2e (~105-135 dk) execute?
   - **Önerim: 3 Tier dahil** — Süleyman'ın v1.4 backlog'da multi-project capability test ZATEN var (~120 dk ayrı session). Tier 3 opsiyonel olarak bu brief'e entegre edilirse aynı session'da bootstrap fix + multi-project smoke verify edilir. Süleyman tek session'da tamamlanmasını isterse Tier 3 dahil.

2. **`workspace_root` fallback strategy:**
   - **Önerim A:** PSEO_WORKSPACE_ROOT yoksa **error** (zorunlu env). En defensive — accidental engine repo path yok.
   - Önerim B: PSEO_WORKSPACE_ROOT yoksa `.` (cwd) fallback. Geriye uyumlu ama F-16 invariant'a yakın iniş.
   - **Önerim: A (error)** — F-16 invariant en sıkı korunma, kullanıcıya net mesaj "PSEO_WORKSPACE_ROOT set edin" ile yönlendirilmiş olur.

3. **Eykom workspace post-fix verify:**
   - Yeni bootstrap modernize ile eykom için **rerun smoke test** yapılsın mı (`/pseo-init test-eykom-rerun --dry-run` → çıktı eykom'un mevcut config ile bire-bir uyum)?
   - **Önerim: EVET, Tier 3'te dahil et** — eykom obsolete-workaround kanıt bunda netleşir (idempotent rerun = workaround artık gerekli değil).

4. **`init-project` SKILL.md §Step 4 update:**
   - Şu an "post-process patch" iddiası SKILL'de yok (eykom için manuel adımdı). Bootstrap modernize sonrası **SKILL.md'ye fix kanıtı** notu eklensin mi (ADR-009 cross-ref)?
   - **Önerim: HAYIR, scope dışı** — SKILL.md edit Tier 4 defer (Süleyman ek session). Bootstrap fix kendi içinde idempotent, SKILL.md edit gerekmez.

### Wave-end onay noktaları (fresh session execute sırasında)

- **Tier 1 sonu:** bootstrap_project.py 7 path modern convention runtime smoke test çıktı sun + schema validate roundtrip kanıt → onay
- **Tier 2 sonu:** Yeni test dosyası pytest output sun + 7 path field assertion coverage matrix → onay
- **Tier 3 sonu (opsiyonel):** 3'üncü proje smoke test çıktısı + eykom rerun obsolete kanıt + cleanup verify → onay
- **Closeout sonu:** OQ RESOLVED marker + PHASE_STATUS banner + memory update sun → onay + push

---

## ⏱️ Estimated Time

- Wakeup: 15 dk
- Tier 1 (script modernize): 30-45 dk
- Tier 2 (test NEW): 45-60 dk
- Tier 3 (multi-project e2e, opsiyonel): 30 dk
- Closeout: 15 dk
- **Total: ~120-150 dk (2-2.5 saat) — Tier 3 dahil ~150 dk**

---

## 📊 Success State (post-Tier-1+2+(3)+closeout)

- bootstrap_project.py: 7 path default LEGACY → MODERN convention
- Engine repo path fallback eliminated (F-16 invariant strict)
- `tests/state/test_bootstrap_project.py` NEW — ~10 test regression-lock
- pytest baseline 743+9 → 753+9 (+10 yeni test positive drift)
- Eykom workaround obsolete — manuel post-process patch gerekli değil
- 3'üncü proje (`test-mp-3` veya benzeri) bootstrap idempotent + modern config (Tier 3 dahilse)
- Q-V1.4-BOOTSTRAP-PATHS-01 RESOLVED — Seçenek A
- Atomic phase paterni 43+44+(45)+46 yeni kanıt; 47-49 phase consecutive convergent invariant intact
- v1.4 milestone içi **ilk feature work** (multi-project capability foundation)
- Brief authoring drift catch (Q metni "mevcut test assertion" iddiası) — Lesson 38 v2 22'inci ardışık vaka cumulative

---

## 🧠 Context Notes (Manager Reference)

### Mevcut state baseline (post-v1.3-Hooks-Overhaul)

| Property | Value |
|---|---|
| Engine HEAD | `7665d4a` PUSHED |
| Pytest baseline | 743 PASS + 9 skip CI baseline |
| Drift-check verdict | AMBER (F-13 historical kalıntı; F-16 + F-17 PASS) |
| .mcp.json size | 482B (F-16 invariant 28+ commit cumulative) |
| DECISIONS.md size | 6112B (32B headroom; 6144B cap ADR-026) |
| Skill count | 43 (active sweep tamamlandı) |
| Command count | 15 |
| MCP server count | 3 (plugin) + 1 (higgsfield user-level) |
| Hook count | 6 (SessionStart + UserPromptSubmit + PreToolUse + PostToolUse + Stop + SubagentStop) |
| Hook test count | 7 (test_active_project_contract impl-agnostik + 6 yeni dosya 68 yeni test) |
| Plugin version | 1.3.0 (ADR-036 5-file sync intact) |
| Repo visibility | PRIVATE (engine + workspace; Wave 3+4 deferred Süleyman karar) |
| Atomic phase paterni | 42'inci kanıt 45 phase consecutive convergent |

### Cross-Audit Metodoloji (3 cephe runtime cross-check)

Bu brief'in kendisi 3 cephede runtime cross-check ürünü:
1. **Repo internal scan:** bootstrap_project.py path defaults + schema paths field + tests/state/ inventory + init-project SKILL.md Step 4
2. **Workspace runtime kanıt:** dentnotion modern convention path inspection (master.xlsx + inbox/sf + _state/cache + outputs/reports + outputs/content/drafts + _state/backups)
3. **Q metni cross-check:** "tests/state/test_bootstrap_project.py mevcut path assertion'ları güncellenmeli" iddiası → runtime grep → dosya YOK → Lesson 38 v2 22'inci ardışık vaka catch (brief'te Tier 2 NEW olarak yazıldı)

**Lesson 38 v2 + Lesson 67 stacked enforcement 22'inci ardışık vaka** (brief authoring kendi içinde): Q-V1.4-BOOTSTRAP-PATHS-01 metninin bir alt-iddiası runtime grep ile invalidate (brief authoring runtime catch — meta-irony kanıt).

### Reference Docs

- **v1.3-hooks-overhaul brief paterni:** `docs/superpowers/plans/v1.3-hooks-overhaul-brief.md` (`0376341` redact + brief authoring `daa19c3`) — 555 satır, 3 Tier + closeout, atomic 4 commit paterni
- **v1.3-marketplace-publication brief paterni:** `docs/superpowers/plans/v1.3-marketplace-publication-brief.md` (`8ff8b88` 371 satır)
- **ADR-009 (master-excel.xlsx schema-driven):** archive — modern convention authority
- **ADR-021 (events.jsonl _state/ path canonical):** archive — `_state/` nested convention authority
- **ADR-035 (PSEO_WORKSPACE_ROOT canonical 1y shim):** archive — env var canonical, no fallback drift
- **ADR-032 (active.json field active_project canonical):** archive — `tests/hooks/test_active_project_contract.py` ile contract-locked
- **Lesson 38 v2 + Lesson 67 stacked:** project_phase_lessons.md memory dosyası — 22 ardışık vaka cumulative

**Cumulative kanıt zinciri (atomic phase paterni):**
- v1.4-Bootstrap-Paths-Fix Tier 1+2+(3)+closeout = 3-4 yeni atomic commit aday
- Cumulative target post-Tier-3+closeout: 46'ıncı kanıt 49 phase consecutive convergent invariant intact

---

## 💡 Hatırlatma — v1.4+ feature backlog

Bu brief **v1.4+ feature backlog'un ilk feature work**'üdür. Sonraki:

- **Multi-project capability test e2e** (~120 dk ayrı session VEYA bu brief'te Tier 3 dahil ~30 dk) — yeni proje onboard pipeline e2e
- **Wave 3+4 marketplace publication execute** (~20 dk ayrı session) — Süleyman PUBLIC karar verirse
- **Süleyman ek items** — TBD

Senaryo X-Revize timeline (sonraki ardışık session'lar):
1. v1.4-Bootstrap-Paths-Fix (this brief, ~120-150 dk)
2. Multi-project capability test (Tier 3'e dahil edilirse bu brief'te tamamlanır)
3. Wave 3+4 publication execute (Süleyman karar bağlı)
4. Süleyman ek v1.4+ item'ları (TBD)

---

**Süleyman feedback enforce:** "çok titiz çalışmanı istiyorum cross checksiz ilerleme" — sürekli runtime cross-check her wave + her edit'te enforce default. Lesson 38 v2 + Lesson 67 stacked enforcement her wave-start zorunlu.

**Başla.** Wakeup sequence + 4 pre-dispatch sorusu sonrası Süleyman onayıyla 2-3 Tier + closeout dispatch.

**Reference:**
- v1.3-hooks-overhaul brief authoring (`daa19c3` + `0376341` redact) → execute (Tier 1+2+3+closeout PUSHED `7665d4a`) — 555 satır paterni reuse
- Q-V1.4-BOOTSTRAP-PATHS-01 [P1] OPEN_QUESTIONS.md line 5-15 — Seçenek A applied
- 7 path default modern convention dentnotion canonical + eykom workaround runtime kanıt
- Brief authoring kendi içinde runtime cross-check (Lesson 38 v2 22'inci ardışık vaka — Q metni "test assertion" iddiası invalidate)
- Tier 1+2+(3 opsiyonel) + closeout = 3-4 atomic commit cumulative aday
