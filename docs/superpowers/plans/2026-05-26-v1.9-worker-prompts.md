# v1.9 Cycle — Worker Prompts (Phase 1..7)

> **Companion file to:** `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` (v1.0)
> **Date:** 2026-05-26 (planning authoring same day as v1.8.0 ship)
> **Author:** v1.8 Manager session (Claude, Süleyman dispatch) preparing v1.9 for fresh session
> **Usage:** Fresh Manager session receives spec + this file + manager-bootstrap; operator copies one Prompt at a time into a fresh Claude Code session per phase. Worker bootstraps via `docs/SESSION_PROTOCOL.md` §13.2; executes ONLY listed scope; returns Worker Output Package per §13.4. Manager processes package, updates state docs, dispatches next Prompt.
> **Serial execution:** Phase N cannot start until Phase N-1 Worker Output Package reviewed and Manager confirms GO.
> **Pattern:** Each Prompt follows `docs/WORKER_PROMPTS.md` Type 1-4 template (Scope, Read-ONLY, Do-NOT-read, Files, Verification, Forbidden, Return).

---

## ⚠️ Pre-Phase-1 Operator Decisions (MUST resolve before dispatching Prompt 1)

| Decision | Default applied if no answer | Locks down |
|----------|------------------------------|------------|
| **Q-V1.9-01** OW-1+OW-2 workshop timing | After v1.9 ship (v1.7 Bank Seed Pilot paterni reuse) | Phase 7 AC-10/AC-13 evidence window |
| **Q-V1.9-02** FE-4 Tier 3 inclusion | DEFER v2.0 (no operator use-case justifies) | Spec scope; v1.9 orchestrator default 24 reports unchanged |
| **Q-V1.9-03** FE-5 F-23 enhancement scope | Additive workspace fallback (backward compat) | Phase 5 implementation approach |
| **Q-V1.9-04** LC-4 ADR-004/005 closure | Close NOW (soak expired 2026-05-12; 14 days post-deadline) | Phase 6 DECISIONS_ARCHIVE.md edit |
| **Q-V1.9-05** SD-1+SD-2 marketplace decision | DEFER v2.0 (separate Süleyman karar cycle) | v1.9 closeout scope (no PUBLIC transition) |
| **Q-V1.9-06** New invariants test pyramid | 4+ test cases per invariant (F-23 paterni reuse) | Phase 2-4 test scope |

If operator silent on any → Manager applies default, logs choice to DECISIONS.md (NO new ADR per D-V1.9-06) + OPEN_QUESTIONS Q-V1.9-PRE-PHASE-1-DECISIONS-01 umbrella entry.

---

# PROMPT 1 — Phase 1: Spec v2.3 Retrospective (R-1..R-10 Consolidated Docs PR)

**Copy below into fresh Claude Code session as the first message:**

```
# Worker Prompt: PSEO v1.9 Phase 1 — Spec v2.3 Retro PR

You are a Fresh Worker Session for the Platinum SEO Engine project. Manager session is operator Süleyman. Bootstrap by reading these files in order (~25KB total, fits <30KB budget):

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 1" (Pre-Phase-1 decisions DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read ONLY: "Category A: Spec v2.3 Retrospective (R-1..R-10)" + "Implementation Phase Outline" Phase 1 row + "Acceptance Criteria" AC-10
3. `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md` — TARGET FILE (you will EDIT this); read sections you'll modify: Risks (R13 mitigation), MCP-Primary Data Flow + 24-Report Loop, Scenario data flows, Implementation Phase Outline (Phase 7), Skills Integration Matrix Category A
4. `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md` — TARGET FILE (you will EDIT this); read Phase 1 task #2 + Phase 6 task #4 verification + Phase 7 §Forbidden + §Tasks task #8
5. `docs/WORKER_PROMPTS.md` — TARGET FILE (you will EDIT this); current Type 2 + Type 3 templates
6. `rules/single-source-of-truth.md` — TARGET FILE (you will EDIT this); current sections list
7. v1.8 OPEN_QUESTIONS source items: `docs/OPEN_QUESTIONS.md` Q-SF-MCP-PHASE-1..5-CLOSURE-FOLLOWUPS-01 entries (R-1..R-9 source)

## Scope
Phase 1: 10 spec retro fixes consolidated as single docs PR (single atomic Manager commit).

## R-1..R-10 Tasks (apply each in target file with cite line numbers in Worker Output Package)

### R-1: Replace `validate_schema.py SKILL.md` verification command (Phase 3 + Phase 6 prompts)
**File:** `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md`
**Change:** Phase 3 prompt verification section (around line 257) — replace `python3 scripts/validation/validate_schema.py skills/ingestion/sf-crawl-orchestrator/SKILL.md` with `python3 -m pytest tests/skills/test_sf_crawl_orchestrator.py::test_frontmatter_validates_against_schema -v` (the working bonus test from v1.8 Phase 3).
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-03 (validate_schema.py needs 2 args; current command exits 0 with usage stderr — false positive).

### R-2: Fix R13 concurrent-crawl guard tool reference
**File:** `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md`
**Change:** Risks table R13 mitigation column (around line 705) — replace `mcp__sf__sf_crawl_progress` with `mcp__sf__sf_list_crawls` (enumerator; doesn't need crawl_id argument).
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-05 (Worker correctly used sf_list_crawls; spec text stale).

### R-3: Spec example DURUR failure code clarification (NOT custom codes)
**File:** `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md`
**Change:** "MCP-Primary Data Flow + 24-Report Loop" section (around line 480-580) — add explicit "Schema-First Note" subsection: "Custom failure code names (sf_mcp_offline, tier1_export_failed, etc.) are illustrative DURUR-NN tokens for `failure_reason.message`. The mechanical `failure_reason.code` field is ALWAYS from the workflow-run.schema closed enum: validation_error / mcp_error / budget_exhausted / user_rejected / timeout / internal_error."
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-06 (Worker preserved schema closed-shape; spec example confusing).

### R-4: events.schema source dict canonical keys callout
**File:** `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md`
**Change:** Data Flow Scenario sections (around line 610-666) — for each `source={...}` example, add inline comment listing allowed keys: `# events.schema source.additionalProperties=false; valid keys ONLY: kind/source_folder/filename_original/filename_normalized/file_hash/row_count/response_bytes/mcp_server/mcp_tool. crawl_id + other arbitrary properties go in inbox/sf-mcp/{date}-sf-crawl-{slug}.json envelope (allows arbitrary).`
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-07 (Worker schema-first wins; spec example schema-violating).

### R-5: Worker Prompts template basename collision rule
**File:** `docs/WORKER_PROMPTS.md`
**Change:** Type 3 (Test Writing Worker) section — add "Basename Collision Rule" subsection: "If the same test file basename appears in BOTH `tests/skills/` and `tests/scripts/` (or any 2 namespace-package directories), pytest default import mode treats them as the same module → collection error. Convention: rename script-side test to `test_<X>_helpers.py` (semantic distinction: helper-layer tests vs skill body tests). DO NOT add `__init__.py` to test dirs — broader pytest config scope."
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-02 (Fix Worker round resolved via rename).

### R-6: Step count semantics clarification
**File:** `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md`
**Change:** Skills Integration Matrix Category A section (around line 100-105) — add "Step Count Semantics" subsection: "When SKILL.md body specs 'N Body Steps', this counts the NUMBERED Body Step prose blocks (typically Step 1 create_run + intermediate workflow steps + Step N complete). The `workflow_runner.create_run(steps=[...])` `steps[]` array typically has FEWER entries because (a) Step 1 (create_run itself) is the entry point not a step; (b) the complete step is a `workflow_runner.complete()` transition NOT in steps[]. Example: orchestrator has 9 Body Steps numbered but 7 entries in steps[] (preflight + crawl_trigger + poll + export_24_reports + atomic_move + invoke_sf_import + emit_provenance_and_report)."
**Cite:** v1.8 Phase 3 Q-PHASE-3-WORKER-04 (Worker interpretation matches dfs-pull/gsc-pull paterni).

### R-7: F-XX namespace rules codification
**File:** `rules/single-source-of-truth.md`
**Change:** Add new "F-XX Namespace Rules" section (toward end of doc): "F-XX cross-sheet invariant labels can appear in TWO disjoint stores: (1) `schemas/cross-sheet-invariants.json` instance entries (persistent registry; audit history references the IDs; ADR-038 monotonic-but-gap-tolerant + renumber FORBIDDEN); (2) `skills/governance/drift-check/SKILL.md` Engine Self-Governance subsection narrative labels (doc-only; no audit history references; ADR-038 renumber-forbidden EXEMPT — these are pedagogical labels not registry IDs). Both stores can use same F-XX numerals if disjoint contexts. v1.8 Phase 6 reconciliation: engine self-governance F-23..F-28 renumbered F-29..F-34 to disambiguate from cross-sheet-invariants.json F-23 SF MCP entry."
**Cite:** v1.8 Phase 4 Q-PHASE-4-WORKER-01 (namespace collision resolved via Phase 6 renumber).

### R-8: Stub-mod vs runtime test pattern clarification
**File:** `docs/WORKER_PROMPTS.md`
**Change:** Type 2 (Skill Implementation Worker) section — add "Stub-Mod vs Runtime Test Pattern" subsection: "When runtime wiring is documented in SKILL.md body prose (executed by skill body interpreter Phase 11+/14 operator workshop) but pure-transform scripts intentionally unchanged: use **stub-mod contract tests** — (a) frontmatter shape test (default value, opt-in flag, schema validation) + (b) SKILL.md body pattern docs test (grep-locked patterns for required body sections). DO NOT write runtime mock tests that would require transform script modification. Runtime verification happens at Phase 7 pilot smoke (AC-13 paterni reuse). v1.8 Phase 5 used stub-mod paterni for 4 consumer skills; Phase 11+/14 workshop covers actual runtime."
**Cite:** v1.8 Phase 5 Q-PHASE-5-WORKER-01 (Worker chose stub-mod correctly; Manager dispatch example test name was illustrative).

### R-9: Phase 7 release commit authorization
**File:** `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md`
**Change:** Phase 7 prompt — (a) §Forbidden section update: "Do NOT make commits EXCEPT the release commit explicitly authorized by task #8a"; (b) §Tasks reword task #8 as two sub-tasks:
- **task #8a (release commit AUTHORIZED):** create release commit with PHASE_STATUS.md update — `git commit -m "release: v1.<X>.0 MILESTONE CLOSED — <title>"`
- **task #8b (tag annotated):** `git tag -a v1.<X>.0 -m "<title>"`; DO NOT push to remote without operator approval
**Cite:** v1.8 Phase 7 workflow ambiguity (Worker resolved defensibly but template tension surfaced).

### R-10: Migration 0005 CLI PROACTIVE clarification
**File:** `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md`
**Change:** Phase 1 prompt task #2 verification section — ADD explicit CLI shape callout: "`scripts/migrations/migration_0005_project_config_1_4_to_1_5.py` uses `--in PATH` (required) + `--out PATH` (optional) + `--dry-run` (flag to invert default-apply behavior). NO `--apply` flag exists for this migration script (distinct from `scripts/release/version_bump.py --apply` Y-05 5-file sync flag, which IS valid). To apply: invoke without `--dry-run` (default mode writes in place + creates .bak). To dry-run: add `--dry-run` flag (writes preview to stdout, source untouched)."
**Cite:** v1.9 live test L-T-1 PROACTIVE clarification (v1.8 worker prompts file does NOT have stale `--apply` reference for migration_0005; R-10 codifies the correct CLI shape inline to prevent future drift / confusion with Y-05 `--apply`).

## Files to MODIFY (consolidated)
1. `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md` (R-2 + R-3 + R-4 + R-6) — ~+30L additive
2. `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md` (R-1 + R-9 + R-10) — ~+25L additive + reword
3. `docs/WORKER_PROMPTS.md` (R-5 + R-8) — ~+40L additive
4. `rules/single-source-of-truth.md` (R-7) — ~+20L additive

## Read ONLY
- 7 bootstrap files
- Target 4 files (read full or relevant sections to find exact edit locations)

## Do NOT read
- Any skill SKILL.md (not in Phase 1 scope)
- scripts/ (not in Phase 1 scope)
- schemas/ (not in Phase 1 scope)
- Other v1.8 OQ entries beyond R-1..R-10 source list

## Verification (RUN BEFORE returning)
```bash
# Confirm all 10 R-XX edits applied via grep cite
grep -l "Schema-First Note\|events.schema source.additionalProperties=false\|Step Count Semantics" docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md
grep -l "sf_list_crawls\|Basename Collision\|Stub-Mod vs Runtime\|F-XX Namespace Rules" docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md docs/WORKER_PROMPTS.md rules/single-source-of-truth.md
grep -l "Migration 0005.*--dry-run\|release commit AUTHORIZED" docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md

# Pytest unchanged (docs-only delta)
python3 -m pytest -q 2>&1 | tail -3
# → expect 1244 PASS / 12 SKIP / 0 FAIL (v1.8 sealed baseline)

# Schema-validate sweep (sanity — docs touched don't affect schemas)
python3 scripts/ci/run_skill_python.py skills/governance/schema-validate/SKILL.md 2>&1 | tail -3
# → EXIT 0
```

## Forbidden
- Touch any code (scripts/ skills/ schemas/) — docs only
- Make commits — leave staged for Manager atomic commit
- Scope creep beyond R-1..R-10 — log new Open Questions for any drift discovered

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: 10 R-XX cite line numbers (file:line) + diff stat per file + grep verification outputs.
```

---

# PROMPT 2 — Phase 2: F-24 Cross-Sheet Invariant (registry ↔ transport key sync)

**Copy below into fresh Claude Code session:**

```
# Worker Prompt: PSEO v1.9 Phase 2 — F-24 Invariant Implementation

Fresh Worker Session. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 2" (Phase 1 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Category B FE-1: F-24" + "Schemas Impact Matrix" + "Scripts Impact" + "Acceptance Criteria" AC-4..6 + Phase 2 row
3. `schemas/cross-sheet-invariants.json` — TARGET schema (additive +1 entry); read existing F-22 + F-23 to understand format
4. `scripts/validation/validate_invariants.py` — TARGET script; read check_F_23 (~line 892-978) for pattern reference; read _RULE_FUNCTIONS + __all__ register patterns
5. `skills/governance/drift-check/SKILL.md` — TARGET skill; read F-23 invariant body section (around line 309+) for pattern reference + invariant count cite locations

## Scope
Phase 2: F-24 implementation end-to-end (schema entry + check function + skill body + 3+ tests).

## Files to MODIFY
1. `schemas/cross-sheet-invariants.json` — Add F-24 entry: `{"id": "F-24", "rule": ".mcp.json mcpServers keys MUST equal mcp-tool-registry.json servers keys (sets comparison; ScraplingServer↔scrapling case-fold per D-SF-02 alignment)", "severity": "HIGH", "category": "engine_consistency", "rationale": "v1.9 Phase 2 D-V1.9-02 cross-sheet invariant — detects drift between .mcp.json transport config + mcp-tool-registry.json tool inventory. Orphan transport (.mcp.json server missing from registry) OR orphan inventory (registry server missing from .mcp.json) → FAIL. ScraplingServer (capital) in .mcp.json normalizes to scrapling (lowercase) in registry per D-SF-02 naming.md alignment.", "computed_by": "consistency_check"}`. Rules array count 28 → 29.
2. `scripts/validation/validate_invariants.py` — Add check_F_24 function (~80L) following check_F_23 paterni (lines 892-978). Logic: read .mcp.json + mcp-tool-registry.json from _REPO_ROOT; normalize ScraplingServer↔scrapling; compute set delta; return FAIL on non-empty delta (PASS otherwise; SKIP if either file missing). Register in _RULE_FUNCTIONS tuple + __all__ export.
3. `skills/governance/drift-check/SKILL.md` — Add F-24 row to HIGH tier invariant table (after F-23 row); add new "F-24 — .mcp.json ↔ mcp-tool-registry.json key sync (v1.9 Phase 2)" subsection in body documenting detection logic. Update count cite "invariants:28" → "invariants:29" in 3 cite locations (description + Step 7 inline comment + audit_payload dict).

## Files to CREATE
4. `tests/skills/test_drift_check.py` extension — Add 3 new test cases:
   - `test_f24_match_pass` — both files have {gsc, dataforseo, scrapling, sf} → PASS
   - `test_f24_orphan_transport_fail` — .mcp.json +"newserver" missing from registry → FAIL
   - `test_f24_orphan_inventory_fail` — registry +"legacy" missing from .mcp.json → FAIL

## Read ONLY
- 5 bootstrap files
- `tests/skills/test_drift_check.py` (read existing F-23 test cases for fixture pattern)
- `tests/schemas/test_cross_sheet_invariants_sync.py` (bidirectional sync test; F-24 entry should auto-validate)

## Do NOT read
- Other skills (Phase 5+)
- scripts/ingestion/ (Phase 3+ scope from v1.8)
- commands/ (Phase 6+ scope)

## Verification (RUN BEFORE returning)
```bash
# F-24 cases
python3 -m pytest tests/skills/test_drift_check.py -v -k "f24 or F_24" 2>&1 | tail -10
# → 3 new cases PASS

# Bidirectional sync test (F-24 auto-validate)
python3 -m pytest tests/schemas/test_cross_sheet_invariants_sync.py -v 2>&1 | tail -5
# → all PASS

# drift-check skill self-run
python3 scripts/ci/run_skill_python.py skills/governance/drift-check/SKILL.md 2>&1 | tail -5
# → EXIT 0; logs "invariants:29" or similar

# Baseline regression
python3 -m pytest -q 2>&1 | tail -3
# → 1244 + 3 new = ~1247 PASS / 12 SKIP / 0 FAIL

# Schema sweep
python3 scripts/ci/run_skill_python.py skills/governance/schema-validate/SKILL.md 2>&1 | tail -3
# → EXIT 0
```

## Forbidden
- Touch F-25 or F-26 (separate phases)
- Touch check_F_23 (Phase 5 F-23 enhancement scope)
- Touch any other skill/script
- Commits

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: check_F_24 function line numbers + schema entry line numbers + 3 test names + pytest output (last 3 lines).
```

---

# PROMPT 3 — Phase 3: F-25 Cross-Sheet Invariant (schema_version coupling)

**Copy below into fresh Claude Code session:**

```
# Worker Prompt: PSEO v1.9 Phase 3 — F-25 Invariant Implementation

Fresh Worker Session. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 3" (Phase 1+2 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Category B FE-2: F-25" + Phase 3 row + AC-4..6
3. `schemas/cross-sheet-invariants.json` — TARGET; F-24 should be added (Phase 2); add F-25 NEXT
4. `scripts/validation/validate_invariants.py` — TARGET; check_F_24 should be added (Phase 2); add check_F_25 NEXT
5. `skills/governance/drift-check/SKILL.md` — TARGET; F-24 row should be added (Phase 2); add F-25 row + body section
6. `schemas/project-config.schema.json` — read schema_version const + sf block structure (v1.5)

## Scope
Phase 3: F-25 end-to-end (schema entry + check function + skill body + 4+ tests).

## Files to MODIFY
1. `schemas/cross-sheet-invariants.json` — Add F-25 entry: `{"id": "F-25", "rule": "if project.config.sf.mcp.enabled=true, project.config.schema_version MUST be >= '1.5' (Migration 0005 prerequisite)", "severity": "HIGH", "category": "csr_mcp", "rationale": "v1.9 Phase 3 D-V1.9-10 — defensive check enforcing schema version coherence. Migration 0005 (v1.4→v1.5) adds sf block; enabled=true on v1.4 is impossible per Migration 0005 lock but defensive check catches manual misconfiguration.", "computed_by": "consistency_check"}`. Rules count 29 → 30.
2. `scripts/validation/validate_invariants.py` — Add check_F_25 function (~70L) following check_F_24 pattern. Logic: read project.config.json from _project_dir; check sf.mcp.enabled boolean + schema_version string; FAIL if enabled=true AND schema_version<"1.5". Register in _RULE_FUNCTIONS + __all__.
3. `skills/governance/drift-check/SKILL.md` — Add F-25 row to HIGH tier table; add new subsection. Update count cite "invariants:29" → "invariants:30" in 3 locations.

## Files to CREATE
4. `tests/skills/test_drift_check.py` extension — Add 4 new test cases:
   - `test_f25_enabled_true_v1_5_pass` — enabled=true + schema_version="1.5" → PASS
   - `test_f25_enabled_true_v1_4_fail` — enabled=true + schema_version="1.4" → FAIL (impossible per Migration 0005 but defensive)
   - `test_f25_enabled_false_v1_4_pass` — enabled=false + schema_version="1.4" → PASS (default behavior valid any version)
   - `test_f25_sf_block_absent_pass` — no sf block at all → PASS (default behavior)

## Read ONLY
- 6 bootstrap files
- tests/skills/test_drift_check.py (existing F-23 + F-24 patterns)

## Do NOT read
- F-24 implementation (already in Phase 2 commit)
- F-26 scope (Phase 4)
- F-23 enhancement (Phase 5)

## Verification (RUN BEFORE returning)
```bash
# F-25 cases
python3 -m pytest tests/skills/test_drift_check.py -v -k "f25 or F_25" 2>&1 | tail -10
# → 4 new cases PASS

# Bidirectional sync
python3 -m pytest tests/schemas/test_cross_sheet_invariants_sync.py -v 2>&1 | tail -5

# drift-check self-run
python3 scripts/ci/run_skill_python.py skills/governance/drift-check/SKILL.md 2>&1 | tail -3
# → EXIT 0; logs "invariants:30"

# Baseline
python3 -m pytest -q 2>&1 | tail -3
# → ~1251 PASS / 12 SKIP / 0 FAIL
```

## Forbidden
- Touch F-24 (Phase 2 done) or F-26 (Phase 4)
- Touch check_F_23 (Phase 5)
- Commits

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: 4 test names + verification outputs.
```

---

# PROMPT 4 — Phase 4: F-26 Cross-Sheet Invariant (orphan SF crawl detection AMBER)

**Copy below into fresh Claude Code session:**

```
# Worker Prompt: PSEO v1.9 Phase 4 — F-26 Invariant Implementation (MCP-Aware)

Fresh Worker Session. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 4" (Phase 1-3 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Category B FE-3: F-26" + Risks R2 (MCP HTTP call hangs) + Phase 4 row
3. `schemas/cross-sheet-invariants.json` — TARGET; add F-26 NEXT (F-24+F-25 already added Phase 2+3)
4. `scripts/validation/validate_invariants.py` — TARGET; add check_F_26 NEXT (MCP-aware, uses sf_mcp_client)
5. `skills/governance/drift-check/SKILL.md` — TARGET; add F-26 row + body section
6. `scripts/util/sf_mcp_client.py` — DEPENDENCY (Phase 2 v1.8 deliverable; F-26 uses SfMcpClient with 1s health probe BEFORE attempting /sf_crawl_progress call)

## Scope
Phase 4: F-26 end-to-end with MCP-aware check (~120L vs F-24/25's ~80L — extra complexity from optional MCP integration).

## Files to MODIFY
1. `schemas/cross-sheet-invariants.json` — Add F-26 entry: `{"id": "F-26", "rule": "if _state/workflows/{run_id}.json shows sf-crawl-orchestrator status=paused|failed AND mcp__sf__sf_crawl_progress({crawl_id}) returns IN_PROGRESS, surface AMBER warning (orphan: workflow state + GUI state disagree)", "severity": "MEDIUM", "category": "csr_mcp", "rationale": "v1.9 Phase 4 D-V1.9-11 — operator-level cleanup hint NOT blocking (AMBER not RED per spec v2.2 line 210). Optional MCP-aware check runs only when sf MCP responding within 1s health probe. SKIP if MCP unreachable (avoid blocking drift-check on MCP downtime).", "computed_by": "consistency_check"}`. Rules count 30 → 31.
2. `scripts/validation/validate_invariants.py` — Add check_F_26 function (~120L) with MCP-aware logic:
   - Read project workflows; collect sf-crawl-orchestrator with status in {"paused", "failed"}
   - If no paused/failed → PASS (vacuous)
   - 1s health probe via SfMcpClient.health(); if FAIL → SKIP (MCP down, don't block)
   - For each paused/failed run with crawl_id: call client.call_tool("sf_crawl_progress", crawl_id=...)
   - If response shows IN_PROGRESS → orphan detected → AMBER warning aggregate
   - severity MEDIUM → AMBER verdict (not RED — operator cleanup hint, not blocking)
   - Register in _RULE_FUNCTIONS + __all__
3. `skills/governance/drift-check/SKILL.md` — Add F-26 row to MEDIUM tier table (NOT HIGH — first MEDIUM entry in v1.9 invariants); add new body subsection documenting MCP-aware optional check + SKIP-on-unreachable pattern. Update count cite "invariants:30" → "invariants:31" in 3 locations.

## Files to CREATE
4. `tests/skills/test_drift_check.py` extension — Add 4 new test cases:
   - `test_f26_no_orphan_pass` — workflow done + GUI clean (mock) → PASS
   - `test_f26_orphan_detected_amber` — workflow failed + mock SF MCP /sf_crawl_progress returns IN_PROGRESS → AMBER
   - `test_f26_mcp_unreachable_skip` — workflow paused + SfMcpClient.health() returns False → SKIP
   - `test_f26_multiple_orphans_amber` — 2 paused workflows + both mocked IN_PROGRESS → AMBER with 2 sample_violations

## Read ONLY
- 6 bootstrap files
- tests/skills/test_drift_check.py (F-23/24/25 patterns)
- tests/scripts/test_sf_mcp_client.py (mock patterns from v1.8 Phase 2)

## Do NOT read
- F-23 enhancement (Phase 5)
- Legacy cleanup (Phase 6)

## Verification (RUN BEFORE returning)
```bash
# F-26 cases
python3 -m pytest tests/skills/test_drift_check.py -v -k "f26 or F_26" 2>&1 | tail -10
# → 4 cases PASS

# Bidirectional sync
python3 -m pytest tests/schemas/test_cross_sheet_invariants_sync.py -v 2>&1 | tail -5

# drift-check self-run (no real SF MCP up = F-26 will SKIP for any orphan scan)
python3 scripts/ci/run_skill_python.py skills/governance/drift-check/SKILL.md 2>&1 | tail -3
# → EXIT 0; logs "invariants:31"

# sf_mcp_client offline behavior unchanged
python3 -m pytest tests/scripts/test_sf_mcp_client.py -v 2>&1 | tail -3
# → 5 PASS (v1.8 Phase 2 baseline)

# Baseline
python3 -m pytest -q 2>&1 | tail -3
# → ~1255 PASS / 12 SKIP / 0 FAIL
```

## Forbidden
- Touch F-23/24/25 (Phase 2+3 done; F-23 enhancement Phase 5)
- Touch sf_mcp_client.py (Phase 2 v1.8 deliverable; F-26 USES it, doesn't modify)
- Commits

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: 4 test names + MCP mock fixture pattern + verification.
```

---

# PROMPT 5 — Phase 5: F-23 Workspace-Aware Enhancement (Architectural Variant)

**Copy below into fresh Claude Code session:**

```
# Worker Prompt: PSEO v1.9 Phase 5 — F-23 Workspace-Aware Enhancement

Fresh Worker Session. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 5" (Phase 1-4 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Category C FE-5: F-23 Workspace-Aware Variant" + Risks R1 (regression risk) + Phase 5 row
3. `scripts/validation/validate_invariants.py` — TARGET; existing check_F_23 (line 892-978) to ENHANCE; verify Phase 2+3+4 added check_F_24/25/26 (do NOT touch those)
4. `schemas/cross-sheet-invariants.json` — TARGET; update F-23 entry rationale text (additive, no schema shape change)
5. `tests/skills/test_drift_check.py` — TARGET; verify existing 3 F-23 cases still PASS BEFORE + AFTER enhancement (regression check)
6. `docs/SESSION_PROTOCOL.md` + `docs/WORKER_PROMPTS.md` (refresher; this is the v1.9 architectural phase, highest regression risk)

## Scope
Phase 5: F-23 additive workspace-aware fallback (NOT breaking refactor). Engine-repo registry stays primary; workspace registry secondary fallback when PSEO_WORKSPACE_ROOT/mcp-tool-registry.json exists. FAIL if EITHER registry missing sf (broad enforcement).

## Files to MODIFY
1. `scripts/validation/validate_invariants.py` — Enhance check_F_23 (~+60L; 40 new + 20 modified):
   - Keep existing logic for engine-repo `_REPO_ROOT / "mcp-tool-registry.json"` as PRIMARY check
   - ADD: workspace registry discovery — if PSEO_WORKSPACE_ROOT env set AND {workspace_root}/mcp-tool-registry.json exists, ALSO load workspace registry
   - ADD: dual-check logic — FAIL if EITHER registry missing sf (broader enforcement; orphan crawl OR missing inventory both surfaced)
   - PRESERVE: existing test fixtures pass unchanged (3 v1.8 F-23 tests baseline)
   - ADD: 3 new code paths for workspace-registry handling (workspace-only + dual-registry-both-pass + dual-registry-workspace-missing-sf)
2. `schemas/cross-sheet-invariants.json` — Update F-23 entry rationale text additive: "v1.9 Phase 5 enhancement: dual-registry check (engine-repo primary + workspace-registry fallback when PSEO_WORKSPACE_ROOT set). Backward compat: engine-repo only deployments work unchanged."

## Files to CREATE
3. `tests/skills/test_drift_check.py` extension — Add 3 new test cases (KEEP existing 3 F-23 cases UNCHANGED):
   - `test_f23_workspace_registry_only_pass` — engine registry missing + workspace registry has sf → PASS
   - `test_f23_workspace_registry_missing_sf_fail` — engine registry has sf + workspace registry missing sf → FAIL (broader)
   - `test_f23_dual_registry_both_pass` — both have sf → PASS

## Read ONLY
- 6 bootstrap files
- Verify existing 3 F-23 tests PASS BEFORE enhancement (regression baseline)

## Do NOT read
- F-24/25/26 implementations (Phase 2+3+4 done)
- Other skills

## Verification (RUN BEFORE returning) — REGRESSION CHECK MANDATORY
```bash
# Baseline check BEFORE enhancement (snapshot)
python3 -m pytest tests/skills/test_drift_check.py -v -k "f23 or F_23" 2>&1 | tail -10
# Expected BEFORE: 3 PASS (existing v1.8 F-23 cases)

# Apply enhancement to check_F_23 (this is the work)
# ... edit scripts/validation/validate_invariants.py ...
# ... add 3 new test cases ...

# Baseline check AFTER enhancement (regression-free verification)
python3 -m pytest tests/skills/test_drift_check.py -v -k "f23 or F_23" 2>&1 | tail -15
# Expected AFTER: 6 PASS (3 existing + 3 new); existing 3 MUST still PASS

# drift-check self-run
python3 scripts/ci/run_skill_python.py skills/governance/drift-check/SKILL.md 2>&1 | tail -3
# → EXIT 0; 31 invariants enumerated

# Full baseline regression
python3 -m pytest -q 2>&1 | tail -3
# → 1255 + 3 new = ~1258 PASS / 12 SKIP / 0 FAIL (Phase 4 baseline + 3)

# Cross-sheet sync
python3 -m pytest tests/schemas/test_cross_sheet_invariants_sync.py -v 2>&1 | tail -5
```

## Forbidden
- Refactor check_F_23 (additive enhancement ONLY; preserve existing logic)
- Touch F-24/25/26 (separate phases)
- Modify existing 3 F-23 test cases (must remain UNCHANGED for regression verification)
- Commits

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: BEFORE + AFTER pytest output (proves regression-free) + 3 new test names + check_F_23 line count delta (e.g., +60L net additive).
```

---

# PROMPT 6 — Phase 6: Legacy Cleanup Batch (LC-1..LC-5)

**Copy below into fresh Claude Code session:**

```
# Worker Prompt: PSEO v1.9 Phase 6 — Legacy Cleanup Batch

Fresh Worker Session. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 6" (Phase 1-5 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Category D Legacy Cleanup Batch (LC-1..LC-5)" + Phase 6 row + Risks R6 (LC-1 WRITER_REGISTRY) + R7 (LC-4 archive byte preservation)
3. v1.2 source items: `docs/OPEN_QUESTIONS.md` Q-W3W2B-WRITER-01 + Q-016 + Q-PHASE15-RXX-COUNT-01 + Q-PHASE15-ADR-CLOSURE-01 entries
4. TARGET files: `scripts/excel/transaction.py` + `scripts/state/events_writer.py` + `rules/events-writer.md` + `docs/DECISIONS_ARCHIVE.md` + various active docs for LC-5 sweep

## Scope
Phase 6: 5 cleanup items bundled atomic per Lesson 38 v2 "same-atomic-commit" discipline.

## LC-1: WRITER_REGISTRY codification (`scripts/excel/transaction.py`)
**Action:** Add `WRITER_REGISTRY` constant enumerating allowed writers per sheet (master_task → master_task_sync; opportunity → quick-wins; quick_wins → quick-wins; etc.). DEFAULT mode = OPT-IN (no enforcement v1.9; flag warning logged); ENFORCEMENT mode reserved v2.0.
**Test:** Add 5+ test cases covering existing writer patterns + WRITER_REGISTRY OPT-IN flag behavior.

## LC-2: audit_action enum mapping refinement (`scripts/state/events_writer.py`)
**Action:** Tighten Edit/Write/Bash → modified mapping in append_audit. Current code may pass tool-specific verbs; refactor to canonical 3-value enum {modified, accessed, deleted}. Schema-first — `events.schema.json` audit_action enum already canonical; code may have stale variants.
**Test:** Add test case verifying Edit → modified mapping; Write → modified; Bash → accessed (READ commands) or modified (write commands).

## LC-3: event_id format documentation (`rules/events-writer.md` Section 5 NEW)
**Action:** Add Section 5 documenting event_id format (currently enforced via schema pattern but not codified in rules). Format: 3-50 chars; alphanumeric + underscores; canonical examples (skill_X_run_N + audit_session_TS + workflow_started).
**Test:** Add `tests/rules/test_event_id_format.py` NEW (1 case verifying rules/events-writer.md Section 5 documents the regex; 1 case asserting events.schema.json event_id pattern matches docs).

## LC-4: ADR-004 + ADR-005 formal closure (`docs/DECISIONS_ARCHIVE.md`)
**Action:** Soak window expired 2026-05-12 (14 days post-deadline at v1.9 dispatch). Append "**Closed:** 2026-05-26 — soak window expired (14 days post-2026-05-12 deadline); v1.4 eski repo silme acceptance criteria met (engine + workspace eski repo silinmiş + 1.6GB recovered per memory feedback_decisions_workflow.md); v1.9 Phase 6 LC-4." to ADR-004 + ADR-005 entries. Preserve original text byte-for-byte (history-stable).

## LC-5: Pre-v1.7 drift sweep (active docs grep)
**Action:** Pre-push audit caught 4 README/ARCHITECTURE stale count drifts (M-1..M-4) + audit Worker confirmed `.claude-plugin/marketplace.json` has stale "43 skills" + "3 MCP servers" cite (pre-v1.7 drift not caught by v1.8 Y-05 sync because Y-05 only touches version+description prefix, not body cite). Run sweep on active docs for similar pre-v1.7 stale references:
- `grep -rnE "\b43 skill|\b44 skill|\b15 (slash )?command|\b16 (slash )?command|\b3 MCP server\b" --include="*.md" --include="*.json" .claude-plugin/ docs/ README.md commands/ rules/ skills/ 2>/dev/null | grep -v "DECISIONS_ARCHIVE\|RELEASE_NOTES_v1\.[1-8]\|PHASE_STATUS\|CONTEXT_LEDGER\|/plans/v1\.[1-7]\|/plans/2026-05-05-phase-15\|/plans/2026-05-26-v1\.9"`
- Fix any stale counts found that should reflect current v1.9 state (45 skills + 18 commands + 4 MCP + post-LC-5 patches)
- **KNOWN DRIFT TARGETS (catch these explicitly):** `.claude-plugin/marketplace.json` body description text "43 skills" + "3 MCP servers"; any other body-text drift not caught by Y-05 5-file sync (which updates only version + description prefix)
**Test:** Add `tests/docs/test_count_consistency.py` NEW (auto-validates active docs against actual file counts via grep + find).

## Files to MODIFY (5)
1. `scripts/excel/transaction.py` — LC-1 WRITER_REGISTRY constant + OPT-IN flag (~+40L)
2. `scripts/state/events_writer.py` — LC-2 audit_action mapping tightening (~+15L)
3. `rules/events-writer.md` — LC-3 Section 5 event_id format NEW (~+25L)
4. `docs/DECISIONS_ARCHIVE.md` — LC-4 ADR-004 + ADR-005 closure append (~+6L total)
5. (LC-5 conditional) Various docs catch any stale references (~+10-30L depending on grep hits)

## Files to CREATE (2)
6. `tests/rules/test_event_id_format.py` — LC-3 (2 cases)
7. `tests/docs/test_count_consistency.py` — LC-5 (auto-validation via grep + find)

## Read ONLY
- 4 bootstrap files
- Target 5 modified files
- Existing audit_action enum in `schemas/events.schema.json`
- Sample of v1.8 LC test patterns (if any)

## Do NOT read
- F-24/25/26 implementations (Phases 2-4)
- F-23 enhancement (Phase 5)

## Verification (RUN BEFORE returning)
```bash
# LC-1 transaction.py test
python3 -m pytest tests/scripts/test_transaction.py -v 2>&1 | tail -5
# (existing tests + new WRITER_REGISTRY tests PASS)

# LC-2 events_writer test
python3 -m pytest tests/scripts/test_events_writer.py -v 2>&1 | tail -5

# LC-3 event_id format new tests
python3 -m pytest tests/rules/test_event_id_format.py -v 2>&1 | tail -5
# → 2 cases PASS

# LC-5 count consistency new tests
python3 -m pytest tests/docs/test_count_consistency.py -v 2>&1 | tail -5

# DECISIONS_ARCHIVE.md grep for closure markers
grep "Closed:.*2026-05-26.*LC-4" docs/DECISIONS_ARCHIVE.md
# → 2 lines (ADR-004 + ADR-005)

# Baseline
python3 -m pytest -q 2>&1 | tail -3
# → ~1265 PASS (Phase 5 baseline +5+ LC tests) / 12 SKIP / 0 FAIL
```

## Forbidden
- Touch F-23/24/25/26 (Phase 2-5 done)
- Breaking change to transaction.py WRITER_REGISTRY (OPT-IN flag DEFAULT off; enforcement v2.0)
- Modify ADR-004/005 original body text (LC-4 append-only)
- Commits

## Return — Worker Output Package
Per SESSION_PROTOCOL §13.4. Include: 5 LC items completion table + 7+ new test names + grep verification outputs.
```

---

# PROMPT 7 — Phase 7: Pilot Smoke + Release (v1.9.0 SHIP)

**Copy below into fresh Claude Code session. FINAL phase.**

```
# Worker Prompt: PSEO v1.9 Phase 7 — Pilot Smoke + Release

Fresh Worker Session. Final phase. Bootstrap reading:

1. `docs/PHASE_STATUS.md` — confirm Active Phase = "v1.9 Phase 7" (Phase 1-6 DONE)
2. `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` — read "Acceptance Criteria" all 20 + Phase 7 row
3. `docs/RELEASE_NOTES_v1.8.0.md` — paterni reference for v1.9 release notes structure
4. `docs/PHASE_STATUS.md` v1.8.0 SHIPPED entry — paterni reference for v1.9 closeout format
5. Operator's recent git log: `git log --oneline v1.8.0..HEAD` (should show 6 v1.9 phase commits)

## Scope
Phase 7: 8 tasks. AC verification + pre-push audit + Y-05 5'inci dogfooding + release notes + git tag + closeout.

## Tasks (sequential)
1. **Full pytest baseline GREEN** — `python3 -m pytest -q 2>&1 | tail -5` → expect ~1265+ PASS / 12 SKIP / 0 FAIL (Phase 1-6 cumulative test additions); record exact count
2. **Drift-check 31 invariants GREEN** — `python3 scripts/ci/run_skill_python.py skills/governance/drift-check/SKILL.md` → EXIT 0; logs "invariants:31" (28 v1.8 + F-24 + F-25 + F-26)
3. **Schema-validate full sweep GREEN** — `python3 scripts/ci/run_skill_python.py skills/governance/schema-validate/SKILL.md` → EXIT 0
4. **Pre-push audit Worker dispatch** (paterni reuse from v1.8 pre-push round) — dispatch Audit Worker (general-purpose subagent) to scan v1.9 deliverables for stale counts + ADR drift + cross-reference integrity + TODO markers; capture findings; if MEDIUM/CRITICAL → fix BEFORE release commit
5. **Y-05 5'inci production --apply** — `python3 scripts/release/version_bump.py --to 1.9.0 --apply` (5-file sync: plugin.json + marketplace.json + README banner + INSTALL banner + RELEASE_NOTES_v1.9.0.md existence check). Verify ADR-036 invariant via `pytest tests/ci/test_version_sync.py -v`.
6. **docs/RELEASE_NOTES_v1.9.0.md NEW** (manual authoring; Y-05 WARN no auto-create; ≥100 lines per v1.7/v1.8 paterni) — structure: Summary + Schema Changes (F-24+F-25+F-26 new invariants) + Migration Notes (no new migration) + Scripts Impact (check_F_NN + transaction.py + events_writer.py) + Tests (~+30 cumulative) + Acceptance Criteria results + Backward Compatibility + Operator Notes (OW-1..OW-3 deferred workshop)
7. **Plugin.json description manual update** — verify Phase 6 LC-5 sweep updated counts if needed; description should reflect post-v1.9 state (likely 45 skill + 18 command + 6 hook + 4 MCP server unchanged from v1.8, since v1.9 added no skills/commands/MCPs)
8. **Rollback drill** — In a temp branch, `git checkout -b v1.9-rollback-drill HEAD` → `git reset --hard v1.8.0` → `python3 -m pytest -q | tail -3` → should restore v1.8.0 baseline (1244 PASS / 12 SKIP). Cleanup: `git checkout main && git branch -D v1.9-rollback-drill`. MUST verify temp branch deleted post-drill.
9. **Release commit (AUTHORIZED per R-9 v2.3 retro applied Phase 1)** — `git add docs/PHASE_STATUS.md docs/RELEASE_NOTES_v1.9.0.md` + Y-05 5-file sync changes → `git commit -m "release: v1.9.0 MILESTONE CLOSED — Spec Retro + 3 New Invariants + F-23 Workspace-Aware + Legacy Cleanup"`
10. **Git tag annotated** — `git tag -a v1.9.0 -m "v1.9.0 — Hardening Cycle (spec v2.3 retro + F-24/25/26 invariants + F-23 enhancement + legacy cleanup)"`. **DO NOT push to remote without explicit operator approval per Manager bootstrap forbidden actions.**

## Files to MODIFY
- `docs/PHASE_STATUS.md` — Update Active Phase to "v1.9.0 MILESTONE CLOSED" with 20-AC evidence block + previous Phase 6 entry demoted
- `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` + `README.md` (banner) + `docs/INSTALL.md` (banner) + `docs/RELEASE_NOTES_v1.9.0.md` (Y-05 5-file sync)

## Files to CREATE
- `docs/RELEASE_NOTES_v1.9.0.md` (NEW ≥100 lines per v1.7/v1.8 paterni)

## Verification (IS the verification)
Each task above includes verification command. Final pre-tag check:
```bash
git log --oneline v1.8.0..HEAD | wc -l
# → 7 (6 phase commits + 1 release commit)

python3 -m pytest tests/ci/test_version_sync.py -v 2>&1 | tail -5
# → PASS (Y-05 5-file sync intact)
```

## Forbidden
- Touch code beyond PHASE_STATUS + RELEASE_NOTES + Y-05 5-file sync targets
- Push tag to remote without operator approval
- Skip rollback drill
- Mark phase complete if ANY AC fails

## Return — Worker Output Package
- Files Created/Modified: 7 files (release commit scope)
- Each of 20 ACs: ✅/❌/⏳ with evidence
- Rollback drill outcome: ✅ baseline restored
- Pre-push audit Worker findings: count + severity
- Git tag created: ✅ (sha)
- Next Step: "operator pushes tag + closes v1.9 milestone OR investigates remaining workshop items (OW-1..OW-3)"
```

---

# Manager Session — Between-Phase Workflow

After each Worker returns a Worker Output Package, Manager (fresh session) does:

1. **Read package** (compact, no full transcript). ~1-2KB.
2. **Verify package against spec Acceptance Criteria** — for ACs the phase touches.
3. **Update state docs:**
   - `docs/PHASE_STATUS.md` — mark phase done, advance Active Phase
   - `docs/DECISIONS.md` — NO new ADR per D-V1.9-06 unless rotation triggered
   - `docs/OPEN_QUESTIONS.md` — log new Q surfaced + close any resolved
   - `docs/CONTEXT_LEDGER.md` — append phase summary (1-2 lines)
4. **Commit atomic** — `git add ... && git commit -m "v1.9 Phase N: <summary>"` per phase
5. **Decide GO/NO-GO** for next phase:
   - GO: dispatch next Worker Prompt
   - NO-GO: spawn fix Worker (narrower scope) before retry — paterni reuse from v1.8 Phase 1 W-1 Fix Worker
6. **Loop** until Phase 7 completes

Manager NEVER:
- Reads worker transcript beyond package
- Writes code directly (always delegates to fresh Worker)
- Edits spec or worker prompts file mid-execution (notes for v1.10 retro)
- Pushes git tag without operator confirmation
- Skips a phase

---

# Operator (Süleyman) Action Checklist

| Step | Action | Where |
|------|--------|-------|
| 0 | Resolve Pre-Phase-1 Operator Decisions table at top of this file (or accept defaults) | Manager session |
| 1 | Approve spec v1.0 + this Worker Prompts file + manager-bootstrap | Manager session |
| 2 | Open fresh Claude Code session for Manager session bootstrap | Operator's terminal |
| 3 | Paste manager-bootstrap content (single block) | Fresh Manager session |
| 4 | After Manager bootstrap complete, decide on Pre-Phase-1 defaults | Manager session |
| 5 | For each Phase 1..7: open fresh Worker session + paste Prompt N + wait for Worker Output Package + paste back to Manager | — |
| 6 | After Phase 7 GREEN: operator pushes git tag (`git push origin main --follow-tags`) + closes v1.9 milestone | Manager session |
| 7 | Schedule OW-1+OW-2 operator workshop (live SF MCP smoke; AC-10+AC-13 evidence post-v1.9 ship) | Workspace repo |

Estimated total operator engagement: 30-60 minutes per phase across 6 days of execution.

---

## Sources / Cross-references

- Spec: `docs/superpowers/specs/2026-05-26-v1.9-retro-invariants-cleanup-design.md` (v1.0)
- Manager Bootstrap: `docs/superpowers/plans/2026-05-26-v1.9-manager-bootstrap.md` (companion file with reading sequence)
- v1.8 paterni: `docs/superpowers/specs/2026-05-26-sf-mcp-hybrid-integration-design.md` + `docs/superpowers/plans/2026-05-26-sf-mcp-worker-prompts.md`
- Session Protocol: `docs/SESSION_PROTOCOL.md` §13.1-13.4
- Worker Templates: `docs/WORKER_PROMPTS.md` Type 1-4
- F-23 paterni reference: `scripts/validation/validate_invariants.py:892-978` (check_F_23 from v1.8 Phase 4)
- Phase Definitions: spec "Implementation Phase Outline" section
- Acceptance Criteria: spec "Acceptance Criteria" (20 items)
