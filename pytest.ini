[pytest]
# Collect only under tests/ — a bare `pytest -q` from a fresh clone gathers
# nothing outside it (the repo has no test_*.py elsewhere). CI-specific flags
# (-q, --tb, matrix, etc.) live in .github/workflows/ci.yml, not here.
testpaths = tests
addopts = -ra
