{
  "mcpServers": {
    "gsc": {
      "command": "bash",
      "args": [
        "-c",
        "set -a; [ -f .env ] && source .env; set +a; exec npx -y mcp-server-gsc@0.3.0"
      ]
    },
    "dataforseo": {
      "command": "bash",
      "args": [
        "-c",
        "set -a; [ -f .env ] && source .env; set +a; exec npx -y dataforseo-mcp-server@2.8.10"
      ]
    },
    "ScraplingServer": {
      "command": "${SCRAPLING_BIN:-scrapling}",
      "args": ["mcp"]
    }
  }
}
